# 主控电脑交付03次：第三轮接口工具阶段 A

分支：`team/main-eval-20260921`。本阶段只完成主控负责的独立收件校验与零分布转换；不代表正式路由或测试结果完成。

## 已完成的技术工作

1. `src/team_eval/snapshot_audit.py` 与 `round3/audit_clean_snapshots.py`：接收四份实际清洗 CSV、数据侧 `task_registry.csv` 与主控 DRAFT 协议，独立核对实际字节 SHA256、四任务身份、行数、三个数值字段、日期严格递增、区间有效、OT 全部有限、配置折边界和输入长度等。仅输出待人工冻结审阅的审计 JSON；工具不会自行改写冻结协议。原始 Time-MMD 到清洗 CSV 的生成链仍需另验。
2. `src/team_eval/null_bridge.py` 与 `round3/convert_nulls.py`：对选择期目录验证清单及每个文件字节哈希、场景、Bundle 签名、仅校准/决策段的样本预测；对两条正式门控候选分别验证 999 条置换损失、连续 iteration/seed、零失败、代码/配置锚点，独立重算决策 MSE 和经验 p，原值转换为主控 `freeze` 接口 JSONL。转换 manifest 标明无法单凭 CSV 证明每次确曾重新拟合，需模型运行日志及代码审阅。
3. `write_stage_inputs` 生成的选择期 manifest 新增 `scenario`，使模型侧 summary 能与主控场景精确对齐。新增 5 项收件负例/正例测试，并检查生成 manifest 的场景。

## 已执行验证

- 在本机 D 盘工作区以 `D:\Users\A\miniconda3\python.exe -B -m unittest discover -s team_work/main/tests -p 'test_*.py' -q` 执行：**29 tests，全部通过**。此环境为 Python 3.14；这些测试覆盖主控桥接合成样例，未执行需 Python 3.12、SciPy、真实大数据与模型训练的全流程。
- 主控 D 盘项目的 `data_processed/team_data/audit-6266b0a931bd32bb166a/` 内四份目标清洗 CSV 均不存在。辅助分支最近已知提交仍为数据 `81e3f2e90ea8c7bbd9358cafcbc5724dff821b9a`、模型 `84e665be9d2e7fa1f6fe946764482e476c7096e9`。因此**未运行真实快照审核、未冻结 `split_spec_v2.json`、未读取正式 Bundle、未转换真实 999 次结果，也未计算路由或测试分数**。

## 收件后执行顺序

1. 数据侧交付四份实际 CSV 后，运行 `round3/audit_clean_snapshots.py --spec team_work/main/round2/split_spec_v2.json --registry <数据侧task_registry.csv> --snapshot-dir <四CSV所在D盘目录> --out <新审计JSON>`。任何不匹配退回数据侧；通过后主控另建冻结协议并记录实际字节哈希。
2. 收到修正后的正式 Bundle，核查其 `targets.npy`、小写 `schema.status=frozen`、完整 manifest 和主控冻结 spec 哈希，再生成选择期输入。不得用重命名伪装修复数据侧协议。
3. 收到模型侧两候选真实 999 次零分布后，运行 `round3/convert_nulls.py --selection-dir <选择期目录> --null-dir <候选一目录> --null-dir <候选二目录> --out <新JSONL>`；通过后仍须审阅置换代码和运行日志，才可让真实结果进入冻结路由。
4. 冻结路由并记录哈希后，才接收测试预测和测试真值进行一次性独立计分；扩展至其余三任务。

**边界：**所有新产物按 D 盘路径保存；只提交主控分支，`main` 未整合。

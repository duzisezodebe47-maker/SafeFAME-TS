# 数据侧第三轮任务书：正式冻结 Bundle 与可转移证据

**负责人分支：**只在 `3218151885-creator` 工作；新交付目录 `辅助电脑02交付03次/`。本分支是无共同祖先的精简快照，不普通合并主控或 `main`，不在其他人分支写文件。固定起点为第二轮交付 `81e3f2e`；旧预览留作历史工程记录。

## 本轮要完成的较大工程

把四任务、两种文本可得性情景的预览管线升级为**可在另一台机器验证和读取的正式数据产品**。本轮不训练模型、不选路由、不写测试收益。

### A. 先交冻结输入，供主控独立核对

1. 交出 `Agriculture`、`Climate`、`SocialGood`、`Environment` 的四份清洗后 `*_numerical.csv` **实际文件**，保留原行顺序、列名、日期与 OT；给每份原始来源、清洗输入、清洗输出的相对路径、SHA256、字节数、行数和生成命令。主控必须能在 D 盘独立重新计算四个清洗文件哈希，不能只收到清单。若不适宜入 Git，可通过 D 盘共享目录/U 盘传递，并在 Git 交付目录记录传输清单与相对路径；禁止使用 C 盘暂存。
2. 不得在四份文件传递后重新清洗并沿用旧哈希。若字节变化，重新生成请求、样本数与全部下游产物。保留 `engineering_preview` 的原始证据，不改写成正式证据。
3. 主控核对通过并发布 `round2/split_spec_v2.json` 的 `status=frozen`、`approved_by` 和四哈希后，以该固定文件的**字节 SHA256**构建。7 档 α 与四任务顺序已由主控确定，不从旧 `team_work/main/protocol/split_spec.json` 取版本。

### B. 修正 Bundle 契约并重建，不手改清单

1. `round2_pipeline.py` 将原始尺度目标写为 `<task>/targets.npy`；如保留 `targets_raw.npy` 供人阅读，也必须同时有字节一致的契约字段 `targets.npy`，并将两者都纳入新 manifest。`numeric_history.npy`、`targets_standardized.npy`、`origin_index.npy`、`samples.csv`、`numeric_fit.json` 路径维持主控契约。
2. 正式 `schema.json` 写精确小写 `"status": "frozen"`；预览保持不可训练状态。正式 manifest 的 `inputs` 增加 `split_spec_sha256`（冻结文件实际字节）和完整 spec 对象，使主控可双重核对。重新计算 Bundle 签名与所有逐文件 SHA256；**不得手改已有 manifest 内哈希**。
3. 更新 `bundle_read_example.py` 和第一轮 `bundle.read_bundle` 的对接，让同一个正式 Bundle 能通过数据侧读取器、模型侧读取器、主控 `team_eval/v2.py` 三方检查。若某读取器属于他人分支，不直接修改；给主控一份可复现的失败路径与建议，主控安排原负责人修复。
4. 对四任务 × `proxy`、`conservative_lag` 全量构建。`complete_source` 只出审计表，覆盖为零，不制造训练数组。每任务给 train/calibration/decision/test 起点数、剔除原因、文本覆盖、独特文本数、目标尺度的均值/标准差，核对 `targets_standardized ≈ (targets-mean)/std`。任何跨段目标窗口、缺失目标或无文本行的处理须留审计。

### C. 真实传输与再核验

1. 通过 `transfer_bundle.py` 将**正式** Bundle 复制到主控可访问的 D 盘位置；复制后在源端和接收端逐文件计算 SHA256，文件清单必须完全相同。交付清单写源提交、主控冻结协议 SHA256、Bundle 签名、文件数、总字节数、目标路径、接收端核验结果、Python 3.12 和依赖版本。
2. 在完整项目上重跑第一轮 19 项、第二轮 15 项，并新增至少这些负例：大写/小写状态误配、缺 `targets.npy`、split spec 字节改变、manifest 多/少文件、起点顺序交换、目标标准化关系错误、传输后单字节改变。报告每项真实退出码与失败信息。
3. 对 Agriculture H12 F1 给至少 3 个起点的原始数值行→清洗行→样本→文本 ID→特征追踪；无文本类别若在该任务不存在，换预注册的 SocialGood 训练段检查，不凭空造一个样例。

## 明确交付物

Git 个人分支 `辅助电脑02交付03次/` 至少包含：`README.md`、修复后的数据管线、`formal_split_spec.sha256`、`clean_snapshot_manifest.csv`、`formal_bundle_manifest.json`、`coverage.csv`、`sample_audit_summary.csv`、`transfer_receipt.json`、`tests_round3.json`、追踪样例和全部执行命令。大文件 Bundle/四份清洗 CSV 可在 D 盘交接，但 Git 目录必须写出可定位的传输方式和接收端核验结果。若接收端尚未完成，不得填 `RECEIVED`。

**期限：**收到本任务后 48 小时内先交四份清洗 CSV 字节与哈希；主控发布冻结 spec 后约 2 天内交正式 Bundle 和传输回执。若原始数据或缓存只能在数据侧机器上访问，及时提供可传输包和校验方法，而非只发本机绝对路径。

**验收门槛：**主控独立计算四哈希与注册值一致；正式 Bundle 经接收端逐字节核验；三方读取器能够读取同一 Agriculture H12 F1，并且样本 ID、数组行序和目标尺度完全一致。通过前继续标记 `DATA_FORMAL_PENDING`。

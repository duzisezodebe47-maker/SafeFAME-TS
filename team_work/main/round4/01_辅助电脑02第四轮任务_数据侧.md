# 辅助电脑02第四轮任务：GitHub 字节交接与正式 Bundle

**只在 `3218151885-creator` 分支工作。**以第三次交付 `d4cf83a` 为起点，新建 `辅助电脑02交付04次/`；保留旧交付目录，不改主控、模型和 `main`。本轮核心工程是让主控实际读到四份清洗 CSV，并完成正式 Bundle。

## 第一阶段：先补齐 GitHub 上的四份实际清洗文件

1. 将 `Agriculture_numerical.csv`、`Climate_numerical.csv`、`SocialGood_numerical.csv`、`Environment_numerical.csv` **实际字节**复制到 `辅助电脑02交付04次/clean_snapshots/` 并推送到本人分支。这四份合计约 531 KB，可直接作为本次交付的小型数据快照；不能只给 D 盘路径、哈希或截图。
2. 从新提交的 Git blob 再算一次 SHA256、字节数、行数、列名，与第三轮 `clean_snapshot_manifest.csv` 和第二轮 `task_registry.csv` 逐项比对。输出 `github_snapshot_receipt.json`，写明**本次固定提交号**、四个 Git 路径、每个 blob SHA256、字节数与命令。若任一字节变更，重新生成登记表和下游请求，不沿用旧哈希。
3. 主控会从固定提交直接提取原始 blob 到自己的 D 盘，再运行独立核验工具。收到主控 `split_spec_v2.json` 的冻结提交和文件 SHA256 之前，不生成正式 Bundle。旧预览和测试夹具不能改名冒充正式数据。

## 第二阶段：主控冻结后构建和传输

1. 读取主控冻结 spec **原始字节**，精确核对 `status=frozen`、`approved_by`、四 CSV 哈希、7 档 α、四任务顺序与协议 SHA256；构建四任务 × `proxy`/`conservative_lag` 正式 Bundle。`complete_source` 只出审计表。
2. 确认 `<task>/targets.npy` 与 `targets_raw.npy` 字节一致、`targets_standardized.npy` 按 train 段均值和标准差计算、`schema.status` 小写 `frozen`、`manifest.inputs.split_spec_sha256` 与主控文件字节一致；对每个正式文件重算 SHA256，拒绝额外/缺失文件。
3. 通过可定位的 GitHub 交付方式上传正式 Bundle。若体积不适宜入普通 Git，可使用仓库 Release 附件或明确的分卷压缩包；给每个附件的 SHA256、总文件清单、解包命令和接收端预期路径。只有主控实际下载到 D 盘并逐文件复核后，回执才可写 `RECEIVED`。不得在 C 盘暂存。
4. 用**正式 Bundle** 重跑数据侧读取器、主控 `team_eval/v2.py` 和模型侧当前读取器，对 Agriculture 至少 3 个起点逐行比较身份、行序、原始/标准化目标与场景。第三轮的 `TEST_FIXTURE_ONLY` 四方测试不能充当正式验收。

## 交付门槛

`辅助电脑02交付04次/` 至少包含四份实际 CSV、`github_snapshot_receipt.json`、冻结 spec 字节哈希、正式 Bundle 文件清单与传输回执、真实三方读取对照、运行命令和失败日志。主控冻结前可先提交第一阶段；第二阶段随主控冻结后继续在**同一分支**提交。时间：第一阶段尽快，建议 24 小时；冻结协议收到后约 48 小时交正式 Bundle。所有状态按实际写 `PENDING`/`COMPLETE`。

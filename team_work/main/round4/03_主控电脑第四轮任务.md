# 主控电脑第四轮任务：独立收件、协议冻结与真实门控

**分支：**只在 `team/main-eval-20260921` 工作，辅助分支只读，不合并 `main`。本轮代码和证据仅写 D 盘。不得凭清单自报或合成测试冻结真实结论。

## 已完成的前置工程

`round3/audit_clean_snapshots.py` 可审实际清洗 CSV；`round3/convert_nulls.py` 已适配模型第三轮新零分布格式并核对场景、候选、999 个序号和种子、成功状态、summary、独立 MSE/p、Bundle/冻结协议锚。主控合成接口测试 31 项通过。

## 后续顺序

1. **从 GitHub 固定提交提取数据侧第四轮四份实际 CSV** 到 D 盘新收件目录，不经 PowerShell 文本重编码；保留原始 blob 字节。对比数据侧清单、主控草稿任务行数/边界、日期、OT 与原始数据清洗规则。任一不符退回；通过后记录审计 JSON、收件 SHA256 和固定提交。
2. 以新协议版本或只增补尚为空的冻结字段，把四实际哈希写入主控 `split_spec_v2.json`，精确 `status=frozen`、`approved_by=team/main-eval-20260921`，记录 UTC、文件 SHA256、主控提交。冻结后禁止原地修改；任何变化开新版本。把固定提交和协议哈希发回数据侧。
3. 收正式 Bundle 后在 D 盘独立核对完整清单、签名输入、每文件哈希、四任务数组及 `targets` 原始/标准化关系。与数据、模型两侧的读取结果在 Agriculture 逐起点对照，必要时做负例。未通过则拒收，不手改其 manifest 或重命名伪装修复。
4. 仅以 calibration/decision 输入导出选择期阶段包；给数值 Last、SeasonalNaive、AR-Ridge、N 和两门控候选做相同起点检查。两条真实 999 次置换通过转换器后，还须核查模型逐次重拟合代码和日志，才调用 `team_eval freeze`。将路由文件**实际字节 SHA256**作为外部锚保存，并明确输出选中候选、数值回退路径、Bundle/协议签名；测试数据不得进入冻结进程。
5. 模型修复真实测试入口并按路由生成测试预测后，主控一次性独立计算逐起点 MSE/MAE、数值回退比较、移动块区间和预测覆盖；与模型侧自报逐数核对。Agriculture 通过后再扩其余三任务和保守滞后情景。

## 交付要求

每阶段产物放 `team_work/main/round4/results/`：收件审计、冻结协议与字节哈希、Bundle 三方对照、选择期预测/null 清单、路由外部 SHA256、测试独立计分、综合状态表。真实输入未到前只提交可审的工具和阻断报告，不能预填效果数字。每轮只推主控分支；整合到 `main` 留到三机闭环验收之后单独进行。

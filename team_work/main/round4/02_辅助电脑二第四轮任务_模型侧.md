# 辅助电脑二第四轮任务：修通冻结路由与真实测试入口

**只在 `SHY` 分支工作。**以第三次交付 `fc8fc39` 为起点，新建 `03 辅助电脑二 交付四次/`；不合并主控整条分支，也不改 `main`。正式 Bundle 尚未到位，先完成以下代码返修和故障测试；Bundle 到位后再做真实运行。

## A. 先修 P0/P1 接口

1. **路由验证：**测试入口读取主控路由文件，要求外部传入的 `--route-sha256` 等于该文件**实际字节** SHA256，并核对路由任务/折、冻结 spec/Bundle 锚、`selection_data_segments=[cal,dec]` 和 `route.selected`。`route.selected` 为门控候选时，只允许该候选；为 `numeric_fallback` 时，明确运行路由指定数值回退模型。不能只比较 JSON 内一个声称的 `route_sha256` 字符串，也不能让该参数可选。
2. **权重重演与扩展：**先按选择期完全相同的 train+calibration 输入、冻结 α 和配置重新拟合，核对选择期 `weight_hash`；确认一致后，按预先规定的 train+calibration+decision 规则扩展重拟合，另存 `test_fit_weight_hash`。禁止拿扩展后的权重与选择期哈希比较。记录两个训练样本集合、行数、输入 SHA256 和模型配置。
3. **Bundle 输入锚：**模型读取器必须重算 `signature(manifest.inputs)`，强制核对主控冻结 spec 的实际字节 SHA256 和嵌入的 spec 对象；继续核对完整文件清单和每个 SHA256。训练、置换、测试入口的 `--split-spec-sha256` 变为必填，最好直接读取冻结 spec 文件路径并自己计算。
4. **分段边界：**从冻结 spec 的四个 `bounds` 向 `assert_grid` 正式调用传入每段半开边界及 H 窗口约束。新增“起点索引格式正确，但跨段目标窗口”的正式入口拒绝测试。
5. **半段一致：**与主控 `team_eval/core.py` 的规则一致：`middle=(cal_end+dec_end)//2`；前段仅 `origin+h<=middle`，后段仅 `origin>=middle`，跨界目标窗口剔除。保留每段起点数和剔除数，不按保留行的中位时间自行改变分界。
6. 更新 `README/RUNBOOK/TEAM_SYNC`：数据侧第三轮已修正式化生成器字段，但正式 Bundle 仍未生成；7 档 α 与四任务顺序已由主控确定，无需再请求裁定。

## B. 正式 Bundle 到位后的真实工程

1. 在主控固定 Bundle 上先跑 `Agriculture_h12_f1/proxy`，输出 N、N+Q、N+S+Q、N+S+Q+SF 的 calibration/decision 逐步预测、每候选配置/权重/代码哈希和运行资源；模型侧自报指标仅用于主控对照。
2. 对两条门控候选各执行 **999 次成功**的逐行联合错位置换，按 `iteration,seed,status,loss,error` 保留每次日志；任何失败未补齐，不报告可用 p。循环移位另列敏感性分析。把两条 null 的 `permutation_summary.json` 和 `null_scores_summary.json` 一起交主控。
3. **只先交选择期证据**，等主控冻结路由并给出实际文件 SHA256 后，再调用修正后的测试入口；不得先看测试真值、让未中选候选跑测试，或根据测试收益改路由。
4. Agriculture 闭环通过后再按 Climate、SocialGood、Environment 扩展；记录每项 `COMPLETE/PARTIAL/NOT_RUN` 和实测资源。特别记录 SocialGood 训练段无文本时语义分支降级，不把质量特征收益说成语义收益。

## 必交验证

新增负例：伪造 JSON 内路由哈希但文件字节改变、外部哈希缺失、任务/候选不符、数值回退被误当门控、选择期与扩展期权重混比、改动嵌入 spec 而文件哈希不变、正式入口跨段窗口。提交全部测试运行命令、退出码和固定提交号。代码修复建议 24 小时内提交；真实运行依赖正式 Bundle 交付，不能用合成 49 项测试冒充。

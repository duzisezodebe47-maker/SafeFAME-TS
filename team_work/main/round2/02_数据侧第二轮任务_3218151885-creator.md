# 数据侧第二轮任务书

**负责人：**分支 `3218151885-creator`，现有交付目录 `辅助电脑02交付01次/`。建议新建 `辅助电脑02交付02次/` 保存本轮交付入口与紧凑证据；代码可延续原目录，但必须给清晰版本和运行入口。只在自己的分支提交，不修改主控、SHY 或 `main`。本轮只做数据和特征工程，不训练模型、不决定路由。

**分支形态补充：**当前 `1cb8ca1` 是与 `main` 无共同祖先的交付快照，只含交付目录。第二次交付可继续在你的分支，但必须注明所依赖的完整项目基线提交，提供把交付目录叠加到完整仓库后的运行步骤和逐文件哈希；不可声称该精简分支单独克隆即可运行，也不要再次无说明地强制改写已经交付的提交。主控将按指定目录选择性接收，不普通整树合并。

## 目标

把第一轮“可生成的 FeatureBundle”推进为**主控协议下可交给模型侧训练的冻结特征包**，先覆盖四项预注册代表任务。每项任务的预测起点、真实目标、原始行来源和特征可得时间都能追溯，产物哈希在交付后不能静默变化。

## 先交给主控的冻结前清单（收到任务后 48 小时内）

对 `Agriculture_h12_f1`、`Climate_h4_f2`、`SocialGood_h3_f1`、`Environment_h7_f2` 逐项给出：清洗后数值 CSV 路径与 SHA256、行数、`input_len`、`horizon`、三折边界四个整数、原始时间频率、未知目标行数。边界必须依据仓库 `configs/safefame_v4.json` 的比例在**清洗后行数**上取整，并由你给出可复核脚本；主控核对后生成正式 `split_spec`。任何任务若原始快照不同于第一轮审计，先提交差异表，不擅自继续。

## 工程任务

1. **对接身份字段：**稳定交付 `task_id`、1 起算 `fold_id`、0 起算 `origin_index`、字符串 `origin_id`、`segment`、`cutoff_time`、H 个 `target_time`、样本保留/剔除理由。任务 ID 与主控统一协议一致，保持样本排序可重复。
2. **冻结两个情景：**`proxy` 与固定日历滞后 `conservative_lag`（日 1、周 7、月 31 天）。`complete_source` 因逐条 URL 缺失，只生成覆盖/排除审计，不输出预测训练集。三情景的目标与数值历史必须相同；只允许文本可得集合变化。
3. **生成正式 Bundle：**使用第一轮 `build/read_bundle` 机制，给四个任务分别输出 `samples.csv`、审计表、`numeric_fit.json`、数值历史与原始/标准化目标、语义/质量/频率数组、缺失掩码、文本覆盖掩码和来源 ID 追踪。保存完整签名及每个文件 SHA256。大数组仍在 D 盘被 Git 忽略的位置，Git 只交清单与小型审计；提供可传递给 SHY 的具体获取方式，不能只写你本机绝对路径。
4. **时点反证：**故意注入晚于 cutoff 的文本、目标窗口跨段、训练外拟合、错位 ID、旧缓存签名、未知目标，要求构建器明确拒绝。抽取每任务至少 3 个起点，其中有文本/无文本/边界各至少一例（若某类不存在，记录原因），输出原始数值行和文本纳入/排除链。
5. **覆盖报告：**按任务×折×情景×段报告候选起点、保留起点、文本可用起点、实际选中文本数、相对 proxy 覆盖损失、未知目标与跨界剔除。对 `SocialGood_h3_f1` 特别验证训练段无文本时语义数组、掩码和后续模型退化所需信号一致。
6. **修正文案：**第一轮 README 的“个人课程设计”应改为三人小组项目；把端点代理的局限、目标单位未核实和 `complete_source` 全空写入交付入口，不能把元数据缺失解释成模型无效。

## 对 SHY 的明确交付接口

给一个只读示例脚本，从冻结 Bundle 读出 `train/calibration/decision/test` 的相同起点顺序、N×L 数值历史、N×H 标准化目标、N×768 语义、N×10 质量、N×10 谱统计、文本覆盖掩码和 `bundle_signature`；同时展示每个样本的原始尺度真值。若需要多变量字段，由 SHY 提出字段清单，主控另立协议版本，本轮不得悄悄混进 OT 接口。

## 必须交付

- `manifest.json`：四任务全部输入/输出 SHA256、Git 提交号、运行命令、环境、耗时、失败项目、是否仍缺大型原始文件。
- 固定的第二次交付提交号/标签、依赖的完整仓库基线提交号，以及选择性导入到完整仓库后的复跑说明。
- `task_registry.csv`：四任务行数、边界、清洗哈希、预计样本数和实际样本数。
- `coverage.csv`、`sample_audit.csv` 样例、3 类起点追踪样例、故障测试结果。
- `bundle_read_example.py` 或等效可执行示例，以及 `README.md` 写清给 SHY 的目录布局与签名校验。
- 如某任务无法构建，交失败清单与原因；不能用工程预览冒充正式 Bundle。

## 主控验收门槛

四任务的清洗哈希与冻结协议一致；跨段目标不重叠、未知目标未被填补；训练参数只来自训练段；代理和滞后情景的样本真值、数值输入逐项一致；更严格滞后不增加“有文本起点”覆盖；读者可在另一台电脑核对完整文件哈希并按签名读取。第一轮记录的 19 项测试继续通过，新增接口和反证测试可复跑。建议收到正式协议后 4 天内交第二轮。

## 可直接给本机 AI 的执行指令

> 只在 `3218151885-creator` 分支继续数据侧工作。先交四任务清洗数值文件哈希和取整边界，由主控冻结正式协议；之后用第一轮管线生成 proxy 与固定滞后情景的正式 Bundle。不得自行选择滞后、修改评价候选或填补未知目标真值。将版本清单、逐文件哈希、样本审计、覆盖表、原始行追踪和可移植读取示例放在 `辅助电脑02交付02次/`。完整数据仍留在 D 盘，Git 上交小型证据并说明另一台电脑怎样取得大型产物。所有测试逐项报告实际运行与失败，不把第一轮工程预览写成正式结果。

# 主控电脑交付 02 次：协议草案与跨电脑对接器

**分支：**`team/main-eval-20260921`。**状态：**`ENGINEERING_PARTIAL`，未完成真实任务闭环。

## 已交付

1. `task_registry.csv` 固定四个工程代表任务。`split_spec_v2.json` 给出取整边界、两种文本情景的滞后、OT 单变量输入、999 次零分布和候选注册，但清洗 SHA256 仍为空，因此明确标为 DRAFT。
2. `prediction_contract_v2.json` 确定字符串起点身份、逐步预测键、目标尺度与哈希字段。旧模型侧 `Climate_h4` 冒烟 CSV 缺少这些字段，会被校验器明确拒绝。
3. `src/team_eval/v2.py` 逐文件核验数据侧 Bundle 清单和字节哈希，再验证起点、时间段、数组行序、边界、训练段拟合的目标标准化关系；逐行拒绝模型预测缺步、重复、起点错位、顺序错位、场景/签名/尺度错配。
4. 用同一份 OT 历史生成 Last、SeasonalNaive 和训练段 Ridge；只用校准段选 Ridge α。按选择期和测试期分文件导出。`write_stage_inputs` 将严格对齐的 v2 输入转换为既有独立评估器可读的隔离阶段包。
5. 既有评分器在测试起点不足一个移动块时输出 `NOT_ESTIMABLE_TOO_FEW_ORIGINS` 和空区间，不再因区间不可估中止计分。
6. `round2/run_bridge.py` 和 `主控对接运行说明.md` 提供真实交付到达后的核验、冻结、计分命令。测试共 24 项通过，其中第二轮新增 14 项；包含用 D 盘合成 Bundle 完成选择包、路由和测试包对接。

本次合成测试实际使用 D 盘 Miniconda 的 Python 3.14.6；仓库原有完整数据/模型管线按 `AGENTS.md` 需要 Python 3.12。本轮没有在 3.12 环境或真实第二轮 Bundle 上复跑，不能把合成测试写成全项目环境验收。

## 仍待交付，不能冒称完成

- 数据侧第二轮四份清洗快照、SHA256 和正式 Bundle 未交。`split_spec_v2.json` **不能冻结**。
- 模型侧尚未提交 v2 逐步预测、真实 SF 交互及 999 次可审计的重拟合零分布。正式路由不能冻结。
- 未运行任何真实第二轮任务，未计算真实测试收益；四任务综合汇总与跨任务族结论均为 `NOT_RUN`。
- 模型侧零分布的生成真实性需要代码、输入和运行日志审计；主控现有入口只对数值执行独立 p 值计算。

## 下一次主控触发条件

数据侧交付后先冻结快照与协议，再核验 Agriculture H12 F1 Bundle；模型侧交校准/决策和零分布后冻结路由；模型侧补交测试期预测后独立计分。任何一步失败都留错误证据退回原分支修复。`main` 尚不整合。

# 辅助电脑二第七次交付指令：封印路由后一次性测试预测

执行分支：`SHY`  
建议目录：`03 辅助电脑二 交付七次/`  
工作量定位：依据主控封印路由，只对唯一候选完成一次不可回看的 Agriculture 测试预测，并把可复现证据交给主控评分。

## 一、唯一授权路由

- 任务：`Agriculture_h12_f1`
- 情景：`proxy`
- 唯一候选：`N+S+Q+SF`
- Bundle 签名：`a69821be115445265cdec0f005686f978de7afb46700edb075dd47aaeee86d38`
- 冻结 spec SHA256：`a0947a5b64d2a609e624c432ef6c6ce9faa6ae8ab86570d4c90594f70c6f0031`
- 路由文件：主控分支 `team_work/main/round7/results/sealed_route.json`
- 路由 SHA256：`8b2c47db4909a755f409eb6e0ea7123b9664b17dbdc3790908ecdd2802df4d70`
- 选择期模型清单：第六次交付 `evidence/cal_dec_predictions/N_S_Q_SF_run_manifest.json`

必须先从主控分支取得路由文件并核对 SHA256。任何一字节变化都停止运行并报告。

## 二、只允许执行一次的主流程

使用第六轮已验收的 `predict_test.py`：

```powershell
python -B "03 辅助电脑二 交付六次/model/predict_test.py" `
  --bundle <正式Bundle目录> `
  --task Agriculture_h12_f1 `
  --scenario proxy `
  --signature a69821be115445265cdec0f005686f978de7afb46700edb075dd47aaeee86d38 `
  --split-spec <主控冻结split_spec_v2.json> `
  --route <主控sealed_route.json> `
  --route-sha256 8b2c47db4909a755f409eb6e0ea7123b9664b17dbdc3790908ecdd2802df4d70 `
  --selection-manifest "03 辅助电脑二 交付六次/evidence/cal_dec_predictions/N_S_Q_SF_run_manifest.json" `
  --candidate N+S+Q+SF `
  --seed 2026 `
  --output-dir "03 辅助电脑二 交付七次/test_prediction"
```

运行前先确认输出目录不存在。若入口失败，只允许修复环境或契约错误后重跑；不得查看 test 真值、误差或排序来修改模型。每次尝试都保留命令、退出码、时间和错误日志。

## 三、测试隔离硬规则

1. 不得读取 Bundle 中 test 段的 `targets*` 文件，不得计算 MSE、MAE、R² 或残差。
2. 不得生成 `N`、`N+Q`、`N+S+Q`、AR-Ridge 等其他测试预测。
3. alpha、PCA、尺度、交互定义和候选结构全部沿用选择期冻结清单，不得重新选择。
4. 允许用 train+calibration+decision 的真值按已实现流程 refit；test 仅作为特征输入。
5. 不得根据预测形状人工剪裁、平滑或替换异常值。若发现非有限值，整次交付失败并保留证据。

## 四、必须交付的证据

- 唯一测试预测 CSV；预计 42 个 origin × 12 步 = 504 行。
- 运行 manifest：候选、任务、情景、四个输入锚、代码提交、工作区状态、alpha、权重哈希、行数、列名、运行时间和峰值内存。
- `test_isolation_audit.json`：证明预测入口未打开 test 真值路径；给出监测方法和读文件清单。
- `route_acceptance.json`：原样记录路由 SHA、selected、status、selection/refit 锚。
- `replay_check.json`：在不读取真值的前提下从相同输入再跑一次，证明预测字节一致、权重哈希一致。第二次运行只用于确定性重放，不用于调参。
- `MANIFEST.json`：本轮所有文件 SHA256。
- `README.md` 和 `RUNBOOK.md`。

## 五、测试与负例

保留并运行第六轮 49 项测试，另增加：

1. 路由 SHA 错误时拒绝。
2. 候选不是 `N+S+Q+SF` 时拒绝。
3. 选择期 manifest 的 alpha 或 weight_hash 改变时拒绝。
4. test 真值路径被访问时拒绝。
5. 输出目录已有文件时拒绝覆盖。
6. 输出行数不是 504、键不唯一、origin 越界或含非有限预测时拒绝。

## 六、交付和分支规则

- 目录名：`03 辅助电脑二 交付七次/`。
- 提交信息含 `03 辅助电脑二 交付七次`。
- 只提交 SHY 分支，不合并 `team/main-eval-20260921` 或 `main`。
- 交付后只报告固定提交 SHA、目录、预测 CSV SHA、manifest SHA 和测试通过数；不要自行报告测试性能。

## 七、主控退回条件

- 运行了多个测试候选，或交付中出现测试评分。
- 路由文件或 SHA 不匹配。
- test 预测不是 504 行，或 key 重复、缺失、越界。
- 使用了新的 alpha、特征、平滑规则或模型结构。
- 工作区不干净，代码提交不是 40 位固定 SHA，或预测无法重放。


# 辅助电脑02第七次交付指令：可重放来源证据与论文数据表

执行分支：`3218151885-creator`  
建议目录：`辅助电脑02交付07次/`  
工作量定位：把第六轮“本机规则一致性审计”升级为“干净电脑可完整重放的数据证据包”，并产出可直接进入技术报告的数据表。

## 一、固定输入

- 以第六次交付固定提交 `f43c3c287165fb0a2b0fc8c15f85e07350252295` 为起点。
- 正式 Bundle 签名保持 `a69821be115445265cdec0f005686f978de7afb46700edb075dd47aaeee86d38`。
- 冻结 spec SHA256 保持 `a0947a5b64d2a609e624c432ef6c6ce9faa6ae8ab86570d4c90594f70c6f0031`。
- 不修改已有 Bundle、数值 CSV、split、模型结果和主控封印路由。

## 二、任务 A：修复 Release 无法独立重跑的问题

第六轮脚本要求 `--audit-dir/fact_corpus.csv`，但 Release 没有交付该文件，`fact_catalog.csv` 又缺少完整质量重算字段。请完成：

1. 在 Release 中加入审计器实际读取的完整规范事实语料，或提供一个不依赖本机缓存、可从固定公开原始版本确定性生成该语料的入口。
2. 给每个输入文件记录相对路径、字节数、行数、列名、SHA256、生成命令和上游版本。
3. 在新建的干净 D 盘目录中只使用 Release 内容运行审计，不得引用仓库外缓存或上一轮目录。
4. 把干净重放命令、退出码、stdout/stderr、总耗时和最终产物哈希写入 `clean_replay/`。
5. Release 资产名称使用新版本号；不要覆盖第六轮附件。提交中给出固定下载链接、资产字节数和 SHA256。

验收标准：主控下载该 Release 后，不提供任何额外 `audit-*` 缓存，仍能从命令行完整生成 `audit_summary.json`、`origin_time_audit.jsonl`、`fact_catalog.csv` 和 `coverage_summary.csv`，结果哈希与交付声明一致。

## 三、任务 B：建立来源证据边界表

不能补造网址。请以现有字段和固定 Time-MMD revision 为依据，生成以下表：

1. `source_evidence_inventory.csv`：至少含 domain、source、source_file、fact_count、start_min、end_max、original_url_count、publication_time_status、upstream_revision。
2. `missing_source_evidence.csv`：列出 source_file 为空、URL 为空、日期只可作代理的数量和比例，按任务、来源分组。
3. `coverage_by_segment_scenario.csv`：按任务、train/calibration/decision/test、proxy/conservative_lag 汇总起点数、文本可用起点、事实数中位数及四分位数。
4. `socialgood_scenario_difference_summary.csv`：把 341 个集合变化与唯一覆盖翻转分开汇总，避免把“集合变了”误写成“全部失去文本”。

每张表必须同时提供 `table_schema.md`，逐列解释统计单位、分母和限制。

## 四、任务 C：形成论文可直接引用的数据审计材料

生成 `paper_ready/`：

- `表_数据来源与样本边界.csv`
- `表_时间边界与覆盖审计.csv`
- `表_来源证据缺口.csv`
- `数据方法与限制说明.md`

文字必须使用边界明确的表述：

- 可写：按冻结规则重算一致；`end_date` 被当作可用时间代理；当前事实没有已登记原始 URL。
- 不可写：已核验全部真实发布时间；全部事实来自可追溯官方网页；文本信息不存在泄漏。

## 五、代码与负例

1. 保留第六轮五个负例。
2. 新增至少四个负例：Release 缺 corpus、corpus 哈希改变、必需列缺失、脚本偷偷读取 Release 外路径。
3. 入口必须在 Windows PowerShell 下用相对路径运行，输出目录可由参数指定。
4. 固定随机种子；结果 JSON 禁止 NaN/Infinity；所有路径使用相对路径写入清单。

## 六、交付清单

- `README.md`：结论、限制、固定输入、复现步骤。
- `RUNBOOK.md`：从下载 Release 到完整审计的一条龙命令。
- `MANIFEST.json`：全部代码和证据文件 SHA256。
- 新 Release 资产及固定下载信息。
- `clean_replay/`、`source_tables/`、`paper_ready/`、`tests/`。
- 分支提交必须干净；提交信息含 `辅助电脑02交付07次`；不得合并主控或 main。

## 七、主控退回条件

- 仍需要上一轮本机 `audit-*` 缓存才能运行。
- 只给结果表，没有完整 corpus 或确定性生成链。
- 为填补来源证据而编造 URL、发布日期或机构归属。
- 表中比例没有分母，或把 proxy 日期写成真实发布日期。
- Release 哈希、Git 清单和重放结果任一不一致。


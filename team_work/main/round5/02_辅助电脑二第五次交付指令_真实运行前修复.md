# 辅助电脑二第五次交付指令：数值回退同源与真实运行

**负责人：**只在 `SHY` 分支工作；从第四次交付 `51a2031` 继续，新目录 `03 辅助电脑二 交付五次/`。不要修改数据侧或主控分支，也不要把主控整条分支并入 SHY。

## A. 正式 Bundle 到位前修复并做端到端负例

1. `predict_test.check_route` 在 `route.selected==numeric_fallback` 时必须要求 `--candidate` **严格等于** `route.fallback`。当前条件允许任何 `GATE_CANDIDATES` 作为请求值却实际执行回退模型，属于旁路。测试应从 `predict_test.main()` 入口传入门控候选，断言非零退出且无预测文件。
2. 统一 `AR-Ridge` 的选路与测试算法。主控已固定实现位于 `team_work/main/src/team_eval/v2.py::numeric_baselines`：全部数值历史窗口作输入、训练段拟合、在 calibration 上选 7 档 α、逐 H 直接输出标准化尺度。当前 `numeric_fallbacks.py` 的末 8 滞后 + train 内部 80/20 选 α 是另一个模型。按主控固定实现改写，并用相同合成 Bundle 在 calibration/decision/test 比对逐起点逐步输出和 α；偏差需在数值容差内。不得根据测试真值改算法。
3. 为 `Last`、`SeasonalNaive`、`AR-Ridge` 设计真实可用的**主控数值基线 manifest**交接方式。当前测试入口对所有模型要求选择期 `run_manifest.json`，但主控基线只导出预测 CSV 和配置哈希，真实回退无法供给该清单。入口应按路由和主控基线固定配置验证，不虚构模型侧清单。`N` 属于模型候选型数值回退，保留其选择期清单并单独测路径。
4. 正式路由必须有 `split_spec_sha256` 和 `bundle_signature` 两锚。测试入口在任一缺失或不一致时拒绝；路由文件本身继续用外部实际字节 SHA256 核对。主控将另行提供含锚的路由产物，旧无锚 smoke 路由不能作为正式测试许可证。
5. 测试预测入口不得向 `predict` 模型提供 test `targets`/`targets_standardized`；构建仅含 test 特征的推理视图。新增故障测试：删除或随机改动测试真值后，预测字节和权重/路由选择不变；评分仍由主控另一个进程读取测试真值。不能只在注释里声称“未使用测试真值”。

## B. 正式 Bundle 到位后真实运行

1. 用主控冻结 spec SHA256 `a0947a5b64d2a609e624c432ef6c6ce9faa6ae8ab86570d4c90594f70c6f0031` 和正式 Bundle 签名在 `Agriculture_h12_f1/proxy` 跑读取与网格审计，再生成 N、N+Q、N+S+Q、N+S+Q+SF 的 calibration/decision 逐步预测和每候选 run manifest。
2. 两门控候选各跑 **999 次成功**的联合逐行错位置换，每次留 iteration/seed/status/loss/error、重拟合配置与失败原因。主控将无损转换并独立重算观测 MSE/p；任何小次数或失败未补齐的结果不得报可用 p。
3. **只先交选择期预测与 null。**主控冻结并哈希路由之后，才按路由执行一次性测试预测。数值回退若被选中，必须使用与主控选路时完全相同的实现。Agriculture 闭环通过后再按既定顺序扩 Climate、SocialGood、Environment 和保守滞后情景。

## 验收门槛

提交新的端到端负例结果与固定代码提交；真实部分给 Bundle/协议/代码/配置/权重哈希、起点数、每次置换日志、资源实测和 `COMPLETE/PARTIAL/NOT_RUN`。代码修复建议 24 小时内先交；真实 Agriculture 依赖数据侧正式 Bundle，不得以 50 项合成测试替代。

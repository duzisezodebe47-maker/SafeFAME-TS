# 主控电脑第五轮任务：正式 Bundle 验收与有锚路由

**只在 `team/main-eval-20260921` 分支工作。**数值快照冻结提交 `6c05bd6`、冻结 spec SHA256 `a0947a5b64d2a609e624c432ef6c6ce9faa6ae8ab86570d4c90594f70c6f0031` 已发布；不再原地改变四哈希或边界，不合并 `main`。所有大型产物和测试暂存放 D 盘。

## 主控现在可独立完成

1. 用四次交付固定 Git blob 的实际字节审计文件保留收件凭证。路由封印工具 `src/team_eval/route_seal.py` 和 `round5/seal_route.py` 已写：从仅含 calibration/decision 的阶段包和 `team_eval freeze` 输出核对 task/spec/prediction/null 输入哈希、Bundle 签名、冻结 spec SHA256、场景与候选身份。必须先有逐次重拟合来源审核，再把路由写成带 `bundle_signature`、`split_spec_sha256`、`scenario` 的正式 JSON。文件实际字节 SHA256 单独输出，不把自身哈希塞进自身 JSON。
2. 路由封印已有合成正例及“缺审核、混入 test、覆盖输出”负例；在真实输入到位后还需执行 null 改字节、Bundle/协议锚不同、候选不在登记的收件验证。主控接口测试当前 33 项通过，不代表真实路由存在。
3. 向模型侧提供数值基线定义与配置锚，尤其 `AR-Ridge` 必须与主控 `v2.numeric_baselines` 一致；为 Last/SeasonalNaive/AR-Ridge 生成可验证的选择期 baseline manifest，使真实回退测试有正式输入。`N` 另走模型候选清单。

## 正式 Bundle 到位后的顺序

1. 从数据侧固定 GitHub 附件把正式 Bundle 提取到主控 D 盘，独立核对附件 SHA256、解包后完整文件清单、`signature(inputs)`、冻结 spec 字节、四任务数组和目标标准化关系。给 Agriculture 至少 3 个起点做三方读取对照；失败退回数据侧，不替其改 manifest。
2. 在隔离目录只导出 calibration/decision 阶段包，核对所有数值基线与模型侧候选预测的 task/fold/segment/origin/step/seed 一一对应；两门控候选各 999 成功置换完成后用主控转换器原值转换，独立复算决策 MSE 和经验 p，再审置换代码与逐次日志。
3. 不提供测试目标或测试预测给 `freeze`；仅满足全部来源检查后冻结、封印路由并把实际文件 SHA256 通知模型侧。模型侧修好的测试入口据此生成唯一中选路径的测试预测；主控随后独立评分。Agriculture 验收后扩另外三任务和稳健情景。

## 交付要求

`team_work/main/round5/results/` 保存正式 Bundle 验收、三方读取对照、逐次 null 转换与审核、带锚路由及外部 SHA256、独立测试计分和综合状态。每个文件标明真实数据覆盖与未跑部分；没有正式 Bundle 前只提交工具与阻断记录。当前已冻结的数值 spec 不能因模型结果而修改。

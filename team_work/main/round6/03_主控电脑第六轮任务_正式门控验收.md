# 主控电脑第六轮任务：正式门控验收

**只在 `team/main-eval-20260921` 分支工作；不合并 `main`。**数据正式 Bundle 已在主控 D 盘接收并核验，签名 `a69821be115445265cdec0f005686f978de7afb46700edb075dd47aaeee86d38`。四任务两情景读取通过；Agriculture/proxy 三方读取通过。主控自己的数值基线已在 `round6/results/agriculture_proxy_baselines/` 生成。

## 主控已完成并须固定的证据

1. Release ZIP 原始字节、93 个清单文件及协议 SHA256 已独立核验；报告 `results/master_formal_bundle_audit.json`。不要重新命名它为数据侧报告；它是主控接收端结果。
2. `SHY@828e7b4` 的 66 项合成测试在固定分离工作树上独立复跑通过；四任务两情景的主控/模型读取器核对通过。Agriculture/proxy 的两个门控候选各导出 1,656 行真实选择期预测，主控网格核对 138 个起点 × 12 步通过。这仅是链路烟雾检查，不能当作完整正式门控结果。
3. 主控数值基线导出工具已在提交 `c7da1db` 固定，Agriculture/proxy 的 Last、SeasonalNaive、AR-Ridge 选择期 CSV 4,968 行；清单给出全部哈希、配置、α=10 与 43 个校准起点。转交模型侧，后续输入绝不根据门控结果反向修改基线定义。

## 辅助电脑二真实结果到位后

1. 固定模型提交和全部附件哈希，校验正式 Bundle 签名、冻结 spec、task/fold/scenario、代码提交、seed、预测网格、run manifest、权重重演。用 N、两门控候选和三个主控数值基线组成仅含 calibration/decision 的选择包；N+Q 等消融单列。
2. 对两门控候选逐条审查 999 次置换日志的成功条件与**实际重拟合调用**，不能只看摘要或 p 值。使用主控转换器无损转换原始 loss，独立重算观察 MSE 和经验 p；若数量、配对、代码锚或重拟合证据不合格，退回修复并保持路由未冻结。
3. 审阅数据侧全量时间边界报告。若发现任一进入模型的事实晚于截止时间，暂停门控并要求重建新的正式 Bundle/签名，既有数值 spec 不得偷偷改写。
4. 所有检查通过后，人工填写 `REFIT_PROVENANCE_VERIFIED` 审核记录，用 `round5/seal_route.py` 从 test-free 选择目录重算并封印唯一正式路由，保存外置 SHA256；通知模型侧。模型侧仅对中选路径生成测试预测，主控再单独读取测试真值评分。未封印前不请求或使用测试预测与测试误差。
5. Agriculture 完整闭环通过后，再按 Climate、SocialGood、Environment 顺序扩展；`conservative_lag` 作为事先登记的稳健情景。重点解释文本覆盖差异，不把不同数据域拼成一个总体。

## 交付状态

`round6/results/` 存放接收报告、读取对照、主控基线 CSV/manifest；之后再追加置换转换、人工审核、路由封印、独立测试评分。当前状态应写 `BUNDLE_ACCEPTED_MODEL_GATE_PENDING`，不得声称最终技术效果已得到老师认可。

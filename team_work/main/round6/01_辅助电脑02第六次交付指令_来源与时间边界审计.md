# 辅助电脑02第六次交付指令：来源与时间边界审计

**只在 `3218151885-creator` 分支工作**，从 `7a28cc5` 继续，交付目录命名 `辅助电脑02交付06次/`。不得改主控、SHY 或 `main`。本轮已收到正式 Bundle；原 Release 附件、签名及冻结 spec 不得原地改写。

## 输入与工程任务

1. 读取主控第六轮 `00_第五次辅助交付验收结论.md` 和 `results/master_formal_bundle_audit.json`，将第五轮回执中的 `MASTER_ACK_PENDING` 在**新回执**中更新为 `MASTER_RECEIVED_AND_VERIFIED`，记录主控验收提交、报告 SHA256 和 Bundle 签名。不要篡改第五轮历史回执。
2. 对四任务的原始文本事实、来源网址、采集/发布/区间日期建立可重放的**起点级时间边界审计器**。逐起点核对：事实结束时间早于预测起点；保守滞后情景还要满足 `end + lag_days < cutoff`；仅允许已登记来源；相同事实不得因重复抓取放大权重。把可用、缺日期、反向区间、超前、滞后未满、来源不合格、截断上限等原因输出为互斥计数。抽样不够，至少对正式 Bundle 中四任务所有保留起点的 `text_trace.jsonl` 做全量核对。
3. 对照正式 Bundle 的 `text_available`、`source_available`、`quality` 与 `text_trace.jsonl`，逐情景核实覆盖率、掩码和质量值的生成规则。重点解释 SocialGood 由 341 降到 340、其余三个任务为何全量可用；记录每个情景的差异起点及触发事实，不能只给汇总百分比。
4. 如果本机留有原始 Time-MMD 缓存，按固定配置重新运行清洗链，把四份 CSV 的实际字节哈希与冻结哈希比较；若没有原始缓存，提交输入缺口、来源路径、所需文件清单和不可复跑原因，不能把已有四份 CSV 再复制一遍写成“原始清洗重现”。
5. 做边界负例：把一条事实改成恰好位于 cutoff、滞后边界、反向日期、重复事实、无原始 URL；审计器应给预期排除原因。负例使用独立副本，不改正式 Bundle。

## 交付与验收

`辅助电脑02交付06次/` 包含审计程序、运行说明、固定输入哈希、全量起点级结果或可下载大文件、四任务 × 两情景覆盖表、SocialGood 差异明细、负例结果、原始清洗重跑或输入缺口回执、新接收状态回执。大文件要给 GitHub 固定下载地址和 SHA256，不能只写你电脑的 D 盘路径。所有结果注明真实执行命令和退出码。

**判定门槛：**主控从固定 GitHub 地址取到审计结果，随机抽查事实、截止时间和掩码可以复算；发现违反时间边界则停止模型门控并报告受影响起点，不得静默改旧 Bundle。建议 48 小时内提交第一版全量审计；原始缓存缺失不阻挡已可独立完成的追踪审计。

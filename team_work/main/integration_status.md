# 三机接入状态（2026-09-21）

| 电脑 | 交付接口 | 当前状态 | 下一步 |
|---|---|---|---|
| 主控 | 协议、校验、独立汇总、冻结路由、测试计分 | 初版已实现；9 项针对性测试通过；静态 v4 审计为 PARTIAL_PASS；合成烟雾样例完整通过 | 接收两方清单后跑真实代表任务 |
| 辅助一 | 数据快照、时点审计、稳定 origin ID、特征清单与 SHA256 | 尚未收到 | 先提供一个领域一个折的 `task.json` 与 `samples.jsonl`；说明日期代理和未知目标处理 |
| 辅助二 | 数值与文本候选逐起点预测、配置和特征哈希、999 次重拟合零分布 | 尚未收到 | 先按同一任务 ID 提供 `predictions.jsonl` 和 `row_nulls.jsonl`；提交校准选参记录 |

当前真实联合任务无法执行，因为两边的文件尚未交付。主控不会用虚构数据填补真实结果。接口冲突由主控记录、三方统一修订协议版本后再运行。

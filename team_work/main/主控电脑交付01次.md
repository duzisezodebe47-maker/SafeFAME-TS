# 主控电脑交付01次

项目：SafeFAME-TS 三人课程设计。交付日期：2026-09-21。负责人：主控电脑。

## 本次交付范围

1. `protocol/split_spec.json`：三折时间划分、目标窗口剔除、冻结路由、检验和区间的机器可读约定。
2. `protocol/prediction_contract.json`：辅助电脑一、二向主控交换任务、样本、预测和置换结果的字段契约。
3. `src/team_eval/`：独立输入校验、冻结路由、测试计分、移动块区间和 v4 紧凑证据核查。
4. `tests/`：10 项针对性测试及可重复生成的人工烟雾样例。
5. `outputs/v4_audit.json`：既有 v4 紧凑证据的审计结果，状态为 `PARTIAL_PASS`。
6. `runbook.md`、`integration_status.md`：运行方法和三机接入状态。

## 实际核验

- `D:\Miniconda3\python.exe -B -m unittest discover -s team_work/main/tests -v`：10 项通过。
- 人工烟雾样例：`freeze` 与 `score` 两阶段均通过；只验证工程接口，不作为论文实验结果。
- 仓库既有 `src/verify_project.py`：`PARTIAL PASS`，对未入库的逐任务证据与模型重放明确跳过。
- v4 紧凑汇总审计：120 项任务折、240 条候选、10 次启用文本、110 次回退；任务折平均相对收益约 0.7676%。这些是已有汇总文件的再核查，不是本轮重训。

## 待两台辅助电脑交付后验收

- 辅助电脑一：逐任务 `task.json`、带时点信息的 `samples.jsonl`、原始数据与特征清单哈希。
- 辅助电脑二：逐起点 `predictions.jsonl`、模型配置哈希、校准记录和决策段 999 次置换结果 `row_nulls.jsonl`。
- 主控随后在一个真实代表任务上先冻结路由、再评价测试，核对来源与哈希后扩展。当前没有真实新任务的逐起点结果，不得宣称全量联合实验完成。

本次交付只包含主控新增文件，未修改旧 v2/v3/v4 正式结果、论文或报告，也未上传 ZIP、环境或大型模型证据。

# 辅助电脑二第八次交付指令：Climate 选择期门控实验

执行分支：`SHY`  
建议目录：`03 辅助电脑二 交付八次/`  
目标：对 `Climate_h4_f2/proxy` 完成选择期候选、消融和两组 999 次置换证据，不运行测试预测。

## 一、固定协议

- Bundle 签名：`a69821be115445265cdec0f005686f978de7afb46700edb075dd47aaeee86d38`。
- 冻结 spec SHA256：`a0947a5b64d2a609e624c432ef6c6ce9faa6ae8ab86570d4c90594f70c6f0031`。
- 任务：`Climate_h4_f2`；fold=2；input_len=52；horizon=4；周期=52。
- 边界：train=636、calibration=763、decision=1017、test=1144。
- 随机种子 2026；alpha 网格和 Agriculture 完全相同。
- 只运行 calibration 与 decision；禁止 test。

## 二、候选与产物

运行并交付：

1. `N`：数值模型候选。
2. `N+Q`：独立消融，只用于解释，不进入正式门控。
3. `N+S+Q`：注册门控候选一。
4. `N+S+Q+SF`：注册门控候选二。

每个候选输出逐步预测 CSV 和 run manifest。预期选择期起点数为 124+251=375，每个候选应有 375×4=1,500 行。

## 三、置换检验

对两个注册门控候选分别执行：

- 999 次逐行错位置换，每次重新拟合 PCA、各分支尺度、交互尺度和 alpha。
- 999 次循环移位敏感性诊断，block 由 Climate 周频结构明确给出并解释，不回写正式 p 值。
- 保存每次 iteration、seed、status、loss、error；失败不能计入成功数。
- 只有成功数达到 999 才报告经验 p 值。
- 报告 decision 前后半段 MSE，不能只给整体值。

## 四、必须修复的流程问题

1. 不得修改第一至第七次历史交付目录；所有修复只放进第八次目录。
2. 实际命令行入口必须做端到端测试，不能只 import 函数。
3. `code_commit` 必须对应运行时干净提交；若先运行后提交，产物作废并在固定提交上重跑。
4. 清单中的 scope 必须写 `03 辅助电脑二 交付八次/model`，不得继续写第六次目录。
5. 测试入口即使退出码为 0但未生成完整产物，也必须判失败。

## 五、测试隔离

- 本轮不得生成任何 Climate test 预测。
- 入口读文件审计中，如果 test 真值被读取为数据而不是完整性哈希，必须判失败。
- 优先消费辅助电脑02提供的选择期隔离包；若该包尚未交付，可先实现代码和合成测试，正式证据必须在固定包上重跑。

## 六、交付清单

- `README.md`、`RUNBOOK.md`、`TEAM_SYNC.md`。
- 四候选选择期预测与 manifests。
- 两门控候选完整 999 次零分布、摘要与运行日志。
- 四段边界审计，但测试段只检查索引和特征结构，不预测、不评分。
- 权重重演、输入读取清单、CPU/内存和总耗时。
- 至少 85 项现有测试继续通过，并新增 Climate 周期、375 起点、1,500 行、禁止 test 的专项测试。
- 全量 `MANIFEST.json`。

## 七、退回条件

- 运行或提交 Climate test 预测。
- 周期仍默认为 12，或没有从冻结 spec 读取 52。
- 置换没有逐次重拟合，或成功次数不足 999。
- 修改历史交付目录。
- 选择期行数、边界、Bundle/spec 哈希不一致。


# 归因审计后全面修改报告

日期：2026-09-20。

## 修改范围

- 三份根目录DOCX及`paper/final`同名DOCX镜像：统一“纯数值残差消融”术语，补充扩展v4后的归因审计、等维纯数值对照、回退锚定对照、频率置换修正、多重校正与解释边界；第4周和第10周材料明确新增内容是事后补记，不冒充当时已完成工作。
- 最终报告：新增第9.6节、表16和图16；更新图10；结论增加“10条启用路线中仅1条满足五项联合事后条件”的边界。核心冻结路由和主实验数值未改写。
- 第10周报告：新增表5并在正文引用；压缩措辞后保持11页，未产生孤立参考文献页。
- `src/run_attribution_audit_v4.py`与`src/verify_attribution_audit_v4.py`：增加五组二阶段对照、块长敏感性、多重校正、跨折描述、修正频率置换和逐起点证据复算。
- `src/verify_project.py`：增加归因审计、表16、DOCX-only版本、镜像和ZIP证据检查；PDF检查仅在`submission.json`启用后执行。
- `src/package_release.py`：纳入归因审计输出，排除报告PDF、历史图PDF、归档、缓存、虚拟环境和嵌套ZIP。
- `README.md`、`submission.json`、`支撑材料清单_v2.txt`及`docs/14`、`15`、`16`、`17`、`20`、`21`、`22`、`23`：统一正式版本、99/999/5000口径、对照名称、运行命令和交付边界。
- `paper/update_attribution_reports.py`、`paper/render_docx_qa.ps1`、`paper/record_submission_qa.py`：保留可重复的DOCX局部修订、临时渲染和QA记录流程；补齐新图替代文本及表头行标记。

## 未修改内容

- 未修改原始数据、历史v2/v3结果、扩展v4的120项冻结路由结果、ETT六项结果和已保存模型权重。
- 未放宽门槛、未新增深度学习模型、未把事后诊断追溯写成预注册主协议。
- 未把相关性写成因果关系，未把`end_date`写成核验后的真实发布时间。
- 未生成当前报告PDF；`paper/final`中的旧报告PDF已移入`paper/archive/20260920_before_attribution_audit`，可恢复但不打包。

## 验证结果

- `src/verify_attribution_audit_v4.py`：PASS，复算10条启用路线、50组因子对照、块长敏感性和修正频率零分布。
- `src/verify_project.py`：PASS，历史v2/v3、扩展v4、归因审计、ETT、协议、报告表格、DOCX镜像与ZIP逐文件哈希一致。
- 三份DOCX逐页渲染检查：第4周9页、第10周11页、最终报告28页；未见空白页、溢出、乱码、孤立图题或表格断裂。
- 三份DOCX无障碍审计：高、中、低风险均为0；新增图片已有替代文本，表格首行已标记为表头。
- 根目录与`paper/final`三份DOCX逐字节一致。临时渲染PDF已删除，当前目录和`paper/final`不含报告PDF。
- 支撑ZIP已按白名单重建；包内`manifest.tsv`与`SHA256SUMS.txt`通过逐文件校验，外部SHA256已重新写入`SafeFAME-TS_v2_支撑材料.sha256`。

## 仍然存在的风险

1. 归因审计使用已查看的历史v4测试块，是事后诊断，不是新的未来独立确认。
2. 等维随机傅里叶对照只匹配输入宽度，不保证有效秩、优化难度或归纳偏置相同。
3. 回退锚定残差头因缺少所有冻结回退的训练段预测，只能在校准段拟合、决策段选参并在校准加决策段重拟合。
4. 240条主门控p值的Holm与BH结果是事后总体描述，不改变逐任务两候选Bonferroni门控。
5. 文献未在本轮重新逐篇联网核验，ETT未全面重训，线上看板仍为历史快照。

## 当前正式文件

1. `SafeFAME-TS_01_选题与初步技术方案（第4周）.docx`
2. `SafeFAME-TS_02_书面中期进展报告（第10周）.docx`
3. `SafeFAME-TS_03_课程设计最终报告.docx`
4. `SafeFAME-TS_v2_支撑材料.zip`
5. `SafeFAME-TS_v2_支撑材料.sha256`
6. `支撑材料清单_v2.txt`

PDF待三份DOCX内容最终确认且收到用户明确指令后再生成。

# 审查问题修订与验证

本文记录v3历史阶段的交付状态。后续v4扩展、页数与支撑包状态以docs/22及当前submission.json为准，下文旧统计不回写成新统计。

日期：2026-09-19。针对上一轮审计实施修订，未换题、未新增模型、未改原始数据。保留历史负结果；新结果单列，不为维持旧数字调整结果。

## 文件修改清单

| 文件或范围 | 修改 |
|---|---|
| src/validation_boundaries.py（新增） | 目标时间隔离校准/决策及决策两半，剔除跨界窗口；无样本子段不得启用，含自检。 |
| src/run_safefame_v2.py | 默认保留v2回放；--purged生成v3，统一cholesky/线程1；测试前存选择记录，保存逐起点预测、99次零分布、全部校准分数、三种子日志与权重。 |
| src/run_reviewer_sensitivity.py | 增加保留旧划分和目标不重叠两套修订入口，统一对齐/移位求解器，保存999次零分布与预测。 |
| src/encode_text.py | 固定MiniLM历史revision，删除动态最新版查询，支持--local-files-only；加强语料/索引校验。 |
| src/verify_revision.py（新增） | 从预测复算门控、指标和区间，加载选中深度回退权重回放；保存验证状态和证据哈希。 |
| src/summarize_revision.py（新增） | 自动生成历史/修订对照JSON、CSV与图。 |
| src/plot_style.py、make_figures.py、summarize_safefame_v2.py、draw_architecture.py、draw_diagnostic_figures.py | 图表字体统一；v2增加仅重绘开关；架构图改正过强的独立未见表述。 |
| 三份根目录DOCX与paper/final镜像 | 保留v2数字，修订独立性和反事实表述；第4/10周补记；最终第9.4节、表12、图12给出新证据；说明ETT的OT评价与通道独立边界。PDF属于当时的历史快照，当前DOCX修订阶段不作为提交版本。 |
| paper/revise_boundary_reports.py、export_submission.ps1、inspect_submission.py、record_submission_qa.py | 本轮局部文档修改、Word导出、渲染、文本比对与审核记录工具；页面检查需显式确认。 |
| outputs/safefame_v2/safefame_v2_protocol.json | 只修解释字段，披露目标重叠及迭代开发限制，不改参数或结果。 |
| outputs/reviewer_sensitivity/reviewer_sensitivity_summary.json | 增加历史lsqr/cholesky混用警示，历史计数不变。 |
| outputs/safefame_v3、reviewer_sensitivity_corrected、reviewer_sensitivity_v3、revision_comparison | 新实验与复核证据独立保存。 |
| README.md、docs/00、02、04、10、11、14、15、16、17、18、19、20 | 版本、运行命令和边界同步；早期计划加历史标记；撤销独立未见、纯对齐反事实、因果/错误率保证等过强解释。 |
| src/verify_project.py | 新增v3证据哈希、计数、正文表与协议检查；保留原有检查。 |
| src/package_release.py、清单、ZIP及SHA256 | 修复包内清单镜像缺失；补充辅助结果及v3权重、日志、预测、零分布；重建白名单包及哈希。 |
| prototype/dist/index.html | 本地看板增加历史版本和修订差异提示，撤销稳定收益保证；未部署在线版。 |

## 新旧结果对照

| 项目 | 历史v2 | 纠错v3 |
|---|---:|---:|
| 任务 / 文本候选 | 18 / 36 | 18 / 36 |
| 校准 / 决策 / 测试起点 | 648 / 655 / 2764 | 571 / 586 / 2764 |
| 完整门槛放行 / 回退任务 | 0 / 18 | 0 / 18 |
| 99次达p≤0.025路径 | 0 | 2 |
| 语义点改善 / 区间正 / 区间负 | 13 / 3 / 4 | 12 / 3 / 2 |
| 频率点改善 / 区间正 / 区间负 | 8 / 2 / 3 | 8 / 3 / 3 |
| 999次达p≤0.025路径 | 0 | 1 |
| 纯数值残差消融区间正 / 负 | 2 / 9 | 2 / 8 |
| 功效诊断不足3块路径 | 32 / 36 | 34 / 36 |

来源：outputs/revision_comparison/summary.json与逐任务CSV。保留旧划分、只统一求解器的中间复核仍999次0/36、匹配区间2正9负。v3剔除146个跨界起点，3项回退专家变化。Climate H24和Economy H12语义99次均p=0.01，但前者不满足两半均胜，后者子段无有效样本；999次仅Climate H24语义p=0.009，不回写路由。“所有单项p都不通过”只属于历史v2，不能搬到v3。

## 实际运行范围

完成v3全部18任务训练与两套修订敏感性共72路径。verify_revision实际通过逐预测值、72行v3指标、全部相应5000次区间、99/999经验p、专家选择和门槛复核，并回放选中深度回退权重。现有MiniLM缓存14281×384通过语料、索引和文件检查，没有重新编码替换缓存。另在HF_HUB_OFFLINE=1且local_files_only=True的条件下，实际加载了固定revision模型，输出维度384；这验证当前机器缓存可离线加载，不代表新电脑不需要下载。

新旧实验分目录，原始数据及历史CSV、数组、模型权重的保护哈希继续核对。DOCX/PDF实测页数、长段落文本比对、实际逐页检查及最终哈希在docs/submission_qa.json记录；最终打包/解压状态见末节。

## 未修改的内容和风险

题目、六领域18任务、模型类别、外层划分、预测跨度、原始数据、历史实验CSV与模型权重、ETT六项12.69%结论未变。99/999/5000用途不变。未制作PPT或汇报材料，未改写成竞赛论文。保留原姓名学号；实际提交日期未提供，未代填。

v3沿用已查看测试集，是事后纠错而非独立确认；边界、求解器和线程共同规范化，不能做单因子因果解释。目标隔离不消除时间依赖，低功效、漂移、发布时间代理、Last锚点不对称、非严格等参数对照仍是限制。经验p没有保证的错误率或因果意义。未干净环境重装、未全面重训ETT、未重新访问全部参考文献网页。大数据及完整历史模型不打包，不能宣称离线完整训练环境。

## 最终正式提交清单

根目录及paper/final同名镜像：

1. SafeFAME-TS_01_选题与初步技术方案（第4周）.docx及同名.pdf。
2. SafeFAME-TS_02_书面中期进展报告（第10周）.docx及同名.pdf。
3. SafeFAME-TS_03_课程设计最终报告.docx及同名.pdf。
4. SafeFAME-TS_v2_支撑材料.zip及.sha256，支撑材料清单_v2.txt。

submission.json为唯一索引。ZIP沿用v2名称是为了提交路径兼容，内容明确含v3。

## 最终交付核验状态

1. 三份DOCX经Microsoft Word正常打开、更新字段并导出PDF；第4周8页、第10周11页、最终报告23页，共42页。逐页渲染检查完成，后续替换图的说明元数据后再次渲染并核对页面；未发现空白页、内容溢出、乱码或孤立图题。课程课件未规定硬性页数上限，最终报告也低于用户一般要求的30页。
2. PDF文件头和实际页数通过；三份报告全部非公式长段落与PDF文本匹配，正文引用与文末文献编号集合一致，图表正文引用通过。核验时修复了跨页文本拼接未插入换行的读取错误，未删除或忽略未匹配段落。DOCX/PDF根目录与paper/final镜像哈希一致。
3. 新图12补充替代文本。三份无障碍扫描均无高优先级问题；最终报告保留一项中等提示：单格算法框并非数据表，不应伪造表头，故保留其结构。视觉检查已覆盖该算法框。
4. 受保护的389个原始数据、历史数值结果和模型文件哈希全部未改变。原始数据未改、历史实验结果未覆盖；新训练与新分析单独保存。
5. src/verify_project.py在完整项目中实际输出PASS。支撑ZIP已按白名单重新生成，共540个文件，含manifest.tsv与SHA256SUMS.txt；外部ZIP SHA256已重新计算，根目录与paper/final两套一致。
6. 实际解压支撑ZIP到全新临时目录，按README补放三份根目录报告及ZIP/SHA256，用系统Python 3.12（不使用项目虚拟环境）运行核验，输出PASS。包内未包含reference_2026.md、虚拟环境、__pycache__、历史归档论文或嵌套ZIP。通过逐文件内容哈希核对，不只是检查文件存在。
7. 当前正式版本以submission.json为准；修改前版本在paper/archive/20260919_before_boundary_fix，可回溯。线上网页未部署更新；当前交付以本地三份DOCX和此次ZIP为准，PDF待DOCX定稿并收到明确指令后再生成。

PASS的范围为上述已执行检查，不等于新样本独立验证、完整ETT重训、全部文献重新联网核验或新电脑干净环境测试。实际提交日期仍需用户按提交时间填写。

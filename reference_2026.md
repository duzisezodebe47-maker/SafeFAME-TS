**2026年西南交通大学**

**数学建模校赛**

**题目**： C （填写 A / B/ C ）

**指导老师： 无 （创新团队必须填写，其他组可填无 ）**

|  |  |  |  |
| --- | --- | --- | --- |
|  | 参赛队员1 | 参赛队员2 | 参赛队员3 |
| **姓名** | 刘羽凡 | 叶锦轩 | 朱梓轩 |
| **学号** | 2025112749 | 2025112340 | 2025112332 |
| **学院** | 交通运输与物流学院 | 计算机与人工智能学院 | 计算机与人工智能学院 |
| **专业** | 交通运输 | 人工智能 | 人工智能 |
| **电话** | 18867190789 | 13540474739 | 19235041239 |
| **QQ** | 2938465907 | 103715447 | 3218151885 |

**西南交通大学教务处**

**西南交通大学实验室及设备管理处**

**西南交通大学数学建模创新实践基地**

**基于多项式回归与多目标分段优化的生物生长规律研究**

**摘要**

本文聚焦于某生物生长规律的研究，基于1000个生长个体的6项核心无量纲生理指标（T、M、Y、X1、X2、X3）的实测数据，综合运用了多种方法——含交叉项的多项式曲面回归、网格搜索分段优化、基于Calinski-Harabasz指数的多目标归一化评分、Bootstrap鲁棒性验证及成本-效率惩罚函数，系统研究了生长指标间的定量关系，并确定了指标M的最优分段策略。

针对问题一，通过**AIC/BIC**准则对四类候选模型进行系统比较，确定含M²T与MT²交叉项的**三次扩展曲面回归模型**为最优方案（模型训练集**R²=0.3290**,5折交叉验证均值为**R²=0.2933**），有效揭示了Y随M增大而降低的趋势、随T增大而升高的非线性曲面规律，进一步的残差分析发现，低M段存在显著异方差性。

对于问题二，在给定固定分段下加权总R²为0.4129；为提升拟合的效果，设计**网格搜索算法**，在满足样本量约束条件下搜索最优分割点，确定为**c₁=19**、**c₂=34**，此时加权总R²提升至0.5740，较整体建模提升38.9%。通过各段残差特征分析，从而揭示了不同生长阶段的异质性规律。

问题三的核心是确定最优分段数。因此，引入**Calinski-Harabasz指数**，度量“段内差异最小”与“段间差异最大”两个目标。将该指数与加权拟合优度经**Min-Max归一化**后，构建**综合评分函数J**，对k=2至6的分段情况进行联合多目标优化，确定最优分段数**k=3**，对应的分段区间为**(0,21]、(21,34]、(34,100)**（此时**J=0.6703**）。**Bootstrap重采样**鲁棒性分析表明分割点的高度稳定性，其95%置信区间分别为[18,21]和[34,39]。

考虑到实际应用中的资源约束，在问题四中，构建了含区间长度偏差惩罚与分段数偏差惩罚的成本-效率目标函数。在基准参数**λ₁=λ₂=0.3**的配置下，最优方案调整为**k=2**、分割点**c=34**。系统灵敏度分析表明，结果呈两区稳定结构，边界清晰，为不同检测资源约束下的实际决策提供了量化依据。

本文有两个较为突出的特点：方法上，将Calinski-Harabasz指数引入分段优化，通过Min-Max归一化使拟合精度与多指标结构合理性在同一框架下可比较，相比于单纯依赖R²选择分段方案，更能兼顾生理阶段的区分度；应用上，构建了含区间长度与分段数惩罚的成本-效率目标函数，结合系统灵敏度分析建立了惩罚系数与最优分段数的对应关系，便于在不同检测资源约束下快速确定合理分段方案。

**关键词：多项式曲面回归 分段建模 多目标优化 Calinski-Harabasz指数网格搜索 Bootstrap重采样灵敏度分析**

1. **问题重述**

1.1问题背景

生物个体的生长过程受多项生理指标的共同调控，个体间存在明显差异，不同阶段亦呈现复杂的动态特征。准确刻画指标间的定量关系，并识别关键指标的分段区间，是理解生物生长规律、指导实际检测工作的基础。

为探究生物生长规律，本研究针对某生物的自然生长过程，共随机抽取1000个生长个体，对6项核心生理指标（T、M、Y、X1、X2、X3）进行权威检测，并且检测数据已完成无量纲化预处理，真实可靠，可直接用于建模分析。初步分析显示，指标M对其他指标的影响呈现分段特征，且在实际检测工作中需要考虑成本与效率，因此对M进行合理分段具有重要意义。

1.2 问题提出

**问题一**：基于数据，分析指标Y与M、T之间的关系，建立合理的关系模型，并评价此模型效果。

**问题二：**要求对指标M进行分段建模分析：首先按给定区间(0,30]、(30,50]、(50,100)构建分段拟合模型并计算总拟合优度；将M分成三段，在确保每段内的个体样本数足量的基础上，通过优化方法确定使总拟合优度最大的最优分割点，并分析各段残差特征。

**问题三**：要求在综合考虑全部6项指标的前提下，联合确定M的最佳分段数及对应分段区间，使得段内差异最小、段间差异最大、Y和M、T的总拟合优度最大三个目标同时满足，并对最终分段结果进行鲁棒性分析。

**问题四**：假设M的分段区间长度与分段数会较大影响检测成本与效率：区间过长或过短均可能导致成本上升，分段数过多或过少也会直接影响检测效率。在兼顾检测成本与效率的前提下，对问题3重新求解，并对结果进行灵敏度分析。

1. **问题分析**

**2.1 问题一分析**

本问要求分析变量Y与指标M、T之间的定量关系，建立关系模型并评价效果。首先需要进行数据分析，把各项指标的基本统计特征、数据分布情况、指标间相关性等可视化处理。接着再重点关注Y与M、T的关系，设计一系列模型，通过AIC/BIC准则进行模型的筛选，来同时控制拟合精度与模型复杂度。在模型评价时先采用R2进行分析，但如果存在少数离群个体，R²受到的干扰变大，单纯依赖R²评价模型可能存在误导，此时再根据需要引入其他指标进行模型评价。

**2.2 问题二分析**

本问包含两个环节：一是基于题目给定的分段区间构建分段拟合模型，二是在三段式框架下确定加权总拟合优度最大化的分割点。第一小问给定分段区间，把问题一所得模型在给定的分段内独立拟合回归方程，之后以各段样本量占比为权重来计算加权总拟合优度，把它与问题一的模型进行对比来验证分段建模策略对提升拟合效果的效果。第二小问要求确定最优化的分段结果，考虑到题目给定的分割点存在主观性，未必贴合数据本身的真实分布规律，为此本文设计网格搜索算法，在满足每段样本量约束的范围内，对所有可行的分割点组合进行遍历计算，构建目标函数分布可视化以定位全局最优解，并通过残差检验与分布分析，验证最优分割点的统计合理性，确保分段结果具备数据支撑。

**2.3 问题三分析**

本问需综合6项指标，同时实现段内差异最小、段间差异最大、拟合优度最高三个目标。首先构建多目标优化框架，将三个目标转化为两项可量化指标：以加权总R2w刻画拟合优度，以Calinski-Harabasz（CH）指数同时度量段内聚集与段间分离，借助CH指数特性合并差异类目标以避免多目标冲突。随后进一步设计归一化通约机制，对量级悬殊的R2w与CH指数分别进行Min-Max归一化，以等权重构建综合得分函数，保障两类目标贡献的公平性。最后验证CH指数在单变量约束分段场景的适用性，通过独立聚类与本文分段结果的一致性检验确认指标可靠性。在此方法的基础上对分段数逐一进行网格搜索，确定最优分段与分割点后，采用重采样法开展鲁棒性分析以验证结论稳定性。

**2.4 问题四分析**

本问在问题三多目标优化框架基础上引入检测成本与检测效率两个约束条件，区间长度与分段数的不合理设置会提高检测成本、降低检测效率。为此本文构建显式惩罚机制，设计区间长度偏差与分段数偏差两项惩罚项，以加权形式从原综合得分中扣除，形成含成本约束的综合目标函数以平滑扩展原有优化框架，再通过网格扫描确定惩罚系数最优取值，可视化参数对最优分段数的影响并建立参数与分段结果的映射关系，为资源约束下的决策提供依据。最后基于更新后的目标函数重新搜索最优分段，分析约束对分段边界、分段数及性能的影响，验证结果合理性。

1. **模型假设**
2. 假设1000个样本来自同一生物总体的独立随机抽样，数据真实可靠，无系统性偏差，数据在无量纲化预处理时未引入额外误差，数据全部真实可靠。
3. 假设M的分割点为整数值，且每段样本量不少于50个。
4. 假设综合评分函数中拟合优度目标与多指标区分度目标权重相等（α=β=0.5），基准惩罚系数λ1=λ2=0.3。
5. **符号说明**

|  |  |  |  |
| --- | --- | --- | --- |
| 符号 | 含义 | 符号 | 含义 |
| T,M,Y | 三项核心生理指标（无量纲） | R2ₖ | 第k段的决定系数 |
| X1,X2,X3 | 三项辅助生理指标（无量纲） | R²\_w | 各段样本量加权总拟合优度 |
| N | 样本总量，n= 1000 | SS\_W | 加权类内总方差（段内差异指标） |
| K | M的分段数 | SS\_B | 加权类间方差（段间差异指标） |
| c₁,c₂,…,c\_{k−1} | M的分割点 | CH | Calinski-Harabasz 指数 |
| J | 多目标归一化综合得分函数 |  |  |

1. **数据探索分析**

**5.1 数据基本统计特征**

本研究数据集共包含1000个生长个体样本，涵盖6项核心生理指标T、M、Y、X1、X2、X3，所有指标均已完成无量纲化预处理。对各指标计算最小值、最大值、均值、中位数、标准差、偏度与峰度共7项统计量[1]。

表1 数据基本特征表

|  |  |  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 指标 | 最小值 | 最大值 | 平均值 | 中位数 | 标准差 | 偏度 | 峰度 |
| T | 0.794 | 92.857 | 32.712 | 27.778 | 22.498 | 0.611 | 2.174 |
| M | 1.905 | 90.658 | 28.113 | 25.347 | 14.023 | 0.954 | 4.188 |
| Y | 22.923 | 74.272 | 32.226 | 32.031 | 3.324 | 4.285 | 51.071 |
| X1 | 4.545 | 90.909 | 36.041 | 36.364 | 15.493 | 0.473 | 3.431 |
| X2 | 12.903 | 87.097 | 54.660 | 51.613 | 16.192 | -0.092 | 2.489 |
| X3 | 2.828 | 87.261 | 28.437 | 26.752 | 12.059 | 0.842 | 4.083 |

由表可知：

1. 数值覆盖范围

T与X1的跨度最宽，均横跨约0至90，个体间差异突出；Y的范围虽达22.9至74.3，但峰度高达51.071，实际上绝大多数个体集中于远比此更窄的区间内；M的均值28.1明显低于上限90.7，右偏特征明显。

1. 离散程度

T的标准差最大（22.498），Y的标准差最小（3.324），仅为T的约1/7两者差异悬殊。X1、X2、X3的标准差介于12至16之间，较为接近。

1. 分布形态

Y的峰度51.071约为标准正态的17倍，是所有指标中分布最为极端的；M与X3均呈右偏（偏度分别为0.954和0.842）；X2最接近对称正态（偏度仅−0.092）；X1的峰度3.431与正态最为接近。

![](data:image/png;base64...)

图1 各核心指标分布箱线图

箱线图直观展示了统计量的判断：T与X1的箱体跨度最大，四分位距分别覆盖约[14,50]与[27,45]，说明个体间差异突出；Y的箱体极为紧凑，四分位距仅约[31,33]，与其峰度高达51.071的统计结果高度一致，但其离群点数量在所有指标中最为密集，说明少数极端个体的Y值远高于主体样本。M的箱体明显右偏，中位数（25.3）显著低于均值（28.1），下须较短而上方离群点较多，进一步印证了M分布的右偏特征。X2的箱体居中且对称性最好，均值与中位数几乎重合，分布最接近正态。X3与M的箱体几乎同形，与两者r=0.83的强相关吻合。各指标的均值（菱形）与中位数（中位线）之间的偏离程度与表2中偏度数值的大小方向完全吻合，两者相互印证。

综合来看，M分布的右偏性与Y的极端尖峰性是后续建模中两个最值得关注的数据特征——前者决定了分段策略的必要性，后者则决定了评价模型效果时不能单纯依赖R²的绝对数值。

**5.2 各指标单独分析**

对6项核心指标分别绘制频数分布直方图，并叠加对应正态参考曲线:

**![各指标分布直方图](data:image/png;base64...)**

图2 频数分布直方图

指标T：分布具有显著的双峰结构，在区间[5,20]与[40,60]附近各存在一个峰值，偏度为0.611，表明生长个体在该指标上可能存在两个集中的生理阶段，单一正态分布无法对其进行充分描述。

指标M：表现为明显的右偏分布（偏度0.954），样本高度集中于低值区间，峰度为4.188，尾部较厚，说明高M值个体数量稀少但存在较大离散性，这种集中于低值、尾部拖长的形态，直接支撑了后续对M分段的必要性。

指标Y：呈现出极度尖峰分布（峰度高达51.071，偏度4.285），绝大多数个体的Y值集中于[30,35]的极窄区间内，但同时存在少量高值极端个体，提示Y的变化可能受到其他指标的强约束。

指标X1：呈轻度右偏（偏度0.473），整体分布较为均匀；

指标X2：为所有指标中唯一表现为轻微左偏的变量（偏度−0.092），分布形态最接近正态；

指标X3：与M的分布形态相近，同样表现为右偏（偏度0.842）且具有较高峰度（4.083），这与两者r=0.83的强相关在分布形态上互相印证。

**5.3 指标间相关性分析**

计算6项指标的Pearson相关系数矩阵[2]，并以热力图形式可视化。随后，单独研究Y与M、T之间的关系为后期建模做准备。

|  |  |
| --- | --- |
| ![相关系数热力图](data:image/png;base64...) | ![Y_M_T三维散点图](data:image/png;base64...) |
| 图3 相关系数热力图 | 图4 Y\_M\_T三维散点图 |

* 由分析热力图的数据可知：M−X3是强关联核心，Y主要受T与M协同驱动，X1独立游离于整个相关网络之外。
* M与X3的相关系数高达0.83，表现出强正相关关系，说明X3是与M最为密切的伴随指标，两者在生理机制上可能存在共同的调控来源；
* X2与X3的相关系数为0.62，呈中等程度正相关，提示辅助指标组内存在一定的信息冗余。
* 就响应变量Y而言，其与T呈正相关（r=0.30），与M呈负相关（r =−0.30），说明T值较大的个体Y值倾向于偏高，而M值较大的个体Y值反而偏低，两者对Y的影响方向相反，这为问题一中构建Y关于M与T的联合模型提供了直接的统计依据。
* 此外，Y与X3的相关系数为−0.26，这一关联很可能是通过M与X3之间的强相关关系间接传导所致，而非X3对Y的直接作用。
* X1是个例外——与其余所有指标的相关系数最大绝对值仅0.04，近乎完全独立，这在后续多指标分析中值得单独关注。
* 由三维散点图直观可得：

图4以颜色深浅映射Y值大小，呈现了Y在M与T联合空间中的分布形态。最突出的特征是Y在垂直方向的极度压缩：绝大多数样本聚集在[25,38]的窄带内，与Y直方图的尖峰分布特征完全吻合。

沿M轴与T轴的趋势差异

* **M轴方向**：低M区（M<30）的样本在Y方向离散程度显著更高，随M增大，Y的分布逐渐收敛，整体呈现缓慢下降趋势。
* **T轴方向**：变化相对平缓，大T值个体的Y均值略高，但变化斜率远弱M方向的影响。

极少数Y>50的高值离群点集中出现在M较小的区域，且与T取值无关，表明这种异常生长状态可能主要由低M水平驱动。

1. **问题一：Y与M、T关系建模**

**6.1 模型思路**

相关系数矩阵与三维散点图共同指向一个问题：Y对M的响应（r=−0.30）与对T的响应（r=0.30）方向相反，且在M较小的区域两者的交互效应尤为突出，数据点的垂直离散程度随M的减小显著放大。这意味着任何忽略交叉效应的线性模型都会在低M区间产生系统性偏差，因此有必要引入高次交叉项来捕捉这一局部非线性结构。为此，本文考虑构建含高次项与交叉项的多项式曲面回归模型，并通过AIC/BIC准则对模型阶数进行系统筛选[3]，找到拟合精度与参数数量之间合理的平衡点。

**6.2 模型建立**

设Y关于M与T的多项式回归模型的一般形式为：

其中ε为随机误差项，β₀至β₇为待估回归系数。该模型包含M与T的一次项、二次项及三次交叉项，共8个参数，既包含M与T各自的非线性主效应，也包含两者之间的二阶交互项。为找出最优模型，本文共设计以下四个候选模型进行多指标比较。

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| 模型 | R2 | AIC | BIC | 参数 |
| 线性模型 | 0.2083 | 5011.47 | 5026.20 | 3 |
| 含交叉项线性 | 0.2658 | 4937.96 | 4957.60 | 4 |
| 完整二次曲面 | 0.2775 | 4926.01 | 4955.46 | 6 |
| 三次交叉扩展 | 0.3290 | 4855.94 | 4895.21 | 8 |

表 3 候选模型比较（AIC/BIC选阶）

由表3明显看出，随参数增多，R²持续上升，AIC与BIC持续下降。三次交叉扩展模型在三项指标上均为最优，且参数数量为8，在1000个样本下参数自由度充足，理论上过拟合风险较低。但需注意，Y分布的极端尖峰性与异方差特征可能导致模型在不同子样本上表现不一致，这一潜在风险已通过6.4节的5折交叉验证加以量化评估。

综合以上三项指标，选定三次交叉扩展模型作为问题一的建模方案。

**6.3 模型求解**

三次交叉扩展模型本质为多元线性回归模型，最小二乘法可直接适配其线性参数结构[4]，在高斯-马尔可夫假设下能得到方差最小的线性无偏估计，同时以最小化残差平方和为目标，与提升拟合优度的建模需求高度一致，且计算稳定、便于后续开展参数显著性检验，因此是该模型参数估计的最优选择。

表 4 模型中的各项系数

|  |  |  |  |
| --- | --- | --- | --- |
| 参数项 | 系数估计值 | 参数项 | 系数估计值 |
| 常数项（β0） | 31.8671 | T2（β4） | −0.0003 |
| M（β1） | 0.0763 | M·T（β5） | −0.0071 |
| T（β2） | 0.1243 | M2·T（β6） | 0.0001 |
| M2（β3） | −0.0034 | M·T2（β7） | 0.0000 |

最终得到的三次交叉扩展模型为：

![拟合曲面图](data:image/png;base64...)

图5 拟合三维曲面图

此模型的R²=0.3290，调整后R²=0.3236，RMSE=2.7211，构建拟合三维曲面来直观分析此模型的拟合效果。曲面在数据主体分布区域与散点吻合较好，但是M较小的左侧边缘因样本稀少，曲面有外推偏移趋势，这与M段异方差问题一致。

**6.4 模型评价**

![残差分析图](data:image/png;base64...)

图6 残差分析图

从四个维度对模型效果进行评价：

1. 拟合精度：R²=0.3290表明模型解释了Y总变异的约33%。考虑到Y本身的峰度高达51.071，极少数高值离群点对总变异的贡献极为突出，模型在主体样本区域的实际拟合效果优于R²数值所直接反映的水平。
2. 残差随机性：由残差与拟合值的散点图（图6左上）可见，残差整体围绕零线分布，无明显的系统性偏差，但在拟合值约[28,35]的区间内残差离散程度明显大于高拟合值区域，提示存在一定的异方差性。
3. 正态性：Q-Q图（图6右上）显示残差在中段与正态参考线吻合良好，但在右尾存在显著的重尾偏离，这与Y分布本身的极端右偏性一致，少量高Y值离群个体导致残差尾部失范。残差直方图（图6左下）同样印证了这一判断，残差整体集中于零附近，但存在明显的右侧拖尾。
4. 异方差性：残差与M的关系图（图6右下）揭示了一个重要规律：在M< 30的低值区间，残差的波动范围显著大于M>40的区间，呈现出明显的漏斗形异方差结构。这一现象与M的分布右偏特征密切相关，低M段样本的生长状态更为多样，Y的不确定性更高，这也是后续问题二中对M进行分段建模的重要动机之一。

综合评价，所建三次交叉扩展曲面回归模型在整体数据层面具有一定的解释能力，能够反映Y随M与T变化的主要趋势，但受限于Y分布的极端特性与低M段的异方差问题，模型的全局拟合精度有待通过分段建模进一步提升。

为进一步评估模型的泛化能力，对三次交叉扩展模型实施5折交叉验证，将1000个样本随机打乱后等分为5折，每次以4折训练、1折测试，循环5次，结果如表5所示。

|  |  |  |
| --- | --- | --- |
| 折次 | 测试集R2 | 测试集RMSE |
| 第 1 折 | 0.5168 | 1.6974 |
| 第 2 折 | 0.1711 | 4.4041 |
| 第 3 折 | 0.1367 | 3.3810 |
| 第 4 折 | 0.2454 | 2.2688 |
| 第 5 折 | 0.3966 | 1.8100 |
| 均值 | 0.2933 | 2.7122 |
| 标准差 | 0.1600 | 1.1565 |

表 5 五折交叉验证相关数据

交叉验证R2为0.2933，低于训练集R²（0.3290），差值为0.0357，说明模型存在一定程度的过拟合。折间差异才是核心问题：第2折和第3折R²分别只有0.171和0.137，而第1折高达0.517，标准差0.1600说明结果高度不稳定。

折间差异的根本来源是少数高Y值离群个体的随机分配，与5.1节中Y的极端尖峰分布分析一致。表6用随机森林做了一个对比基准，输入特征同样是M和T，验证方案与表5一致。

表 6 多项式模型与随机森林5折交叉验证对比

|  |  |  |
| --- | --- | --- |
| 折次 | 多项式模型 R | 随机森林 R |
| 第 1 折 | 0.5168 | 0.7914 |
| 第 2 折 | 0.1711 | 0.2358 |
| 第 3 折 | 0.1367 | 0.4647 |
| 第 4 折 | 0.2454 | 0.5739 |
| 第 5 折 | 0.3966 | 0.8973 |
| CV 均值 | 0.2933 | 0.5926 |
| CV 标准差 | 0.1600 | 0.2629 |

随机森林的交叉验证均值R2为0.5926，显著高于多项式模型的0.2933，这说明M与T对Y确实包含较强的非线性预测信息，多项式模型的低R2主要源于其函数形式的表达局限，而非变量选择存在问题。两种模型的折间不稳定性根源一致：少数高Y值离群个体所在的折，会直接拉高该折的R2表现，进而导致不同折之间结果波动。

尽管随机森林的整体预测精度更高，本文仍保留多项式模型，核心原因在于其对生物生长指标间的相互作用具备可解释性优势：多项式模型的每个系数都能直接对应M与T对Y的具体作用方式，完美契合“解释Y波动机制”的建模目标；而随机森林作为黑箱模型，无法为后续分段分析提供可解读的残差与变量效应。此外，分段后各段内部的多项式模型R2可达0.57，已接近随机森林的整体水平，两者精度差距大幅收窄，兼顾了解释性与预测性能。

1. **问题二：M的分段拟合分析**

**7.1 固定分段拟合模型**

**7.1.1 各段模型建立**

题目给定的三段划分(0,30]、(30,50]、(50,100)为本文提供了一个基准方案。将1000个样本按M值归入对应区间后，各段分别独立估计三次交叉扩展模型的8个参数，段间参数不共享，各区间独立估计。各段样本量分别为602、321、77个，满足参数估计的基本样本量要求。

**7.1.2 总加权拟合优度计算**

表 7 各段拟合相关数据

|  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| 分段 | 样本量 | R2 | 调整R2 | RMSE | 权重 |
| (0,30] | 602 | 0.3711 | 0.3627 | 1.7085 | 0.6020 |
| (30,50] | 321 | 0.4061 | 0.3909 | 2.7086 | 0.3210 |
| (50,100) | 77 | 0.7675 | 0.7402 | 2.9050 | 0.0770 |
| 加权总R2 | 1000 | 0.4129 | - | - | - |

以各段样本量占总样本量的比例为权重，计算加权总拟合优度：

与问题一整体建模结果（R2=0.3290）相比，固定分段后加权总R2提升了0.0839，提升幅度约25.5%，说明分段建模对拟合效果带来了实质性改善，而非边际优化。

从分段拟合表现看，三段之间R2差异显著：高M区间(50,100)的R2高达0.7675，表明该区间内Y与M、T呈现高度规律的关联；中间区间(30,50]拟合效果居中，R2为0.4061;低M区间(0,30]拟合效果最弱，R2仅为0.3711，这与第四章残差分析发现的“低M段异方差”现象高度吻合——低M个体生长状态本身更具多样性，与残差图中低M段漏斗形异方差是同一问题的两种表现。同时，各段RMSE随M区间增大而上升，也侧面反映高M段虽拟合优度更高，但单样本预测误差更大，这与样本量减少、区间范围变窄有关。各段残差散点图如图7所示。

![固定分段残差图](data:image/png;base64...)

图7 固定分段残差图

(0,30]段残差围绕零线对称分布，整体随机性较好，但存在少量高残差离群点，反映了该段Y值的高度离散性；(30,50]段残差随拟合值升高而系统性减小，说明该段存在模型未能捕捉到的非线性结构；(50,100)段整体残差最小，分布最为紧凑，但样本量仅77个，高R²有一定过拟合嫌疑。

(0,30]覆盖了60%的样本，R²却最低——这说明问题不在分段策略本身，而在分割点的位置选得不好，7.2节针对这一点做网格搜索。

**7.2 最优分段搜索**

**7.2.1 优化算法设计——网格搜索**

本问的核心目标是确定使模型总拟合优度最大化的最优分割点，将该目标形式化为：在满足每段样本量不少于50个的约束条件下，寻找分割点组合(c₁​, c₂​)（c₁​<c₂），使加权总R2w达到全局最大值。为避免样本稀疏区段（如高M段仅77个样本）对整体评价指标产生过度干扰，加权总R2w​采用各分段样本量为权重进行计算，而非等权求和，使评价结果更能反映模型在主体数据上的实际拟合表现。

设定两个分割点和，并限定搜索范围：c₁∈[10,45]、c₂∈[15,80]，步长均为1，同时满足c₁<c₂的逻辑约束，共生成约1200种有效分割点组合。针对每一种组合，分别构建三段独立的三次交叉扩展模型，计算对应的加权总R2w​，并遍历所有组合以记录全局最优解。

为直观可视了所有有效组合下加权总R²w的分布格局，下表为R²w分布等高线图红色区域对应高R²w​方案，五角星标注全局最优点。

![](data:image/png;base64...)

图8 网格搜索R²w分布等高线图

由图可知以下信息：

高值区域分布：R2w​的高值区域集中于c₁​∈[18,22]、c₂∈[30,38]的较小范围内，表明最优解附近存在稳定的高值区间，模型结果不会因分割点小幅移动而骤变。

梯度变化特征：在全局最优点(c₁​​=19,c₂=34)附近，等高线排列紧密、梯度变化显著，说明该区域内分割点位置的选择对R2w​影响较大；而在远离最优点的区域，等高线稀疏，R2w​变化平缓。

有效搜索空间：灰色区域为不满足每段样本量≥50约束的无效组合，整体有效搜索空间呈三角形分布，符合c₁<c₂的约束逻辑。

**7.2.2 最优分割点结果**

搜索结果如下表所示：

表 8 最优分段结果

|  |  |  |
| --- | --- | --- |
| 分段 | 样本量 | R² |
| (0,19] | 287 | 0.3361 |
| (19,34] | 412 | 0.8065 |
| (34,100) | 301 | 0.4827 |
| 加权总R² | 1000 | 0.5740 |

本研究通过最优分割点算法确定了两个关键分界点c₁=19与c₂=34，将总样本量为1000的数据集划分为(0,19]、(19,34]及(34,100)三个子区间，各子区间样本量分别为287、412与301，样本分布相对均衡。分段拟合结果显示，中间区间(19,34]的决定系数R2高达0.8065，表明该区间内模型对因变量变异的解释力极强，可解释约80.7%的变异；首尾区间(0,19]与(34,100)的R2分别为0.3361和0.4827，虽解释力相对较弱，但仍能捕捉到部分非线性关系。以各子区间样本量为权重计算的加权总R2为0.5740，较传统固定分段方案的0.4129提升了38.9%，这一结果证实了最优分割点策略在挖掘变量非线性关联、提升整体模型拟合精度方面的显著优势，为后续的机制分析与预测建模提供了更稳健的统计基础。

|  |  |
| --- | --- |
| ![](data:image/png;base64...) | ![052b36b654fe4f9d7d91daa471bbc750](data:image/png;base64...) |

|  |  |
| --- | --- |
| 图9 M的核密度分布与最优分割点示意图 | 图10 分段R²对比图 |

为直观呈现最优分割点在M分布上的合理性，绘制M的核密度估计曲线并标注分割点位置，由图9可见，M的分布整体呈右偏单峰形态，峰值出现在M≈19附近，与第一分割点c₁=19高度吻合，说明该位置恰好处于样本最密集区域的右缘，是区分低M段密集个体与中M段核心个体的天然边界。第二分割点c₂=34位于密度曲线下降段的中部，将拟合效果最优的核心区间(19,34]与高M段(34,100)有效分离。三段的样本量分别为287、412、301个，分布较为均衡，不存在某段样本量过少导致模型不稳定的问题。三段颜色区域的面积直观反映了各段的样本占比，中间段（黄色）覆盖了密度曲线最高的核心区域，与其最高R²（0.807）相对应，分割点的位置与数据分布的自然结构吻合，选取合理。

从拟合效果来看，M值处于[19,34]区间的个体，其Y与M、T之间的关系最为规律，R²高达0.8065，说明该生长阶段的调控机制相对集中稳定；而低M段(0,19]和高M段(34,100)的拟合效果相对较弱，两端区间R²偏低，说明仅靠M与T两项指标不够，需要引入更多指标信息，这也是问题三扩展至6项指标的直接原因。

**7.3 残差特征分析**

问题一的残差分析发现了显著的分段性异方差，而分段建模是否真正解决了这一问题，需要通过各段内部的残差结构加以验证。最优分段各段残差如图11所示，三段的残差标准差分别为2.78、1.12、3.90，与问题一全局模型的整体RMSE（2.72）相比，中M段(19,34]下降了59%，中M段的残差收窄幅度最为突出，分段策略在这里效果最明显。

![9183239668fe58140accc8b5a07a354d](data:image/png;base64...)

图11 最优分段残差图

1. (0,19]段残差整体集中在零附近，绝大多数点的残差绝对值小于2，但存在若干极端正残差离群点（最大值约30），分布呈现显著的右偏尖峰特征。这与该段Y值分布本身的极端性一致，少数低M值个体的Y异常偏高，是本身数据的极端情况，与模型设计无关。
2. (19,34]段残差分布最为紧凑，波动范围基本控制在[-4,6]之间，围绕零线呈近似对称分布，随机性最好，无明显的系统性偏差，与其最高R²（0.8065）的表现相互印证。
3. (34,100)段残差的波动范围明显扩大，部分残差绝对值超过20，且在高拟合值区域出现较多离散点。结合该段样本量（301个）横跨M值[34,100]的宽广区间来看，单一模型在覆盖如此大范围的M值时仍存在一定的局限性，这也为问题三中进一步细化分段数提供了依据。

三段对比下来，(19,34]是分段效果最好的区间，两端仍有残差不稳定的问题。单靠M与T两项指标已经达到上限，问题三的方向就是把剩余4项指标也纳进来。

1. **问题三：多指标联合最优分段**

**8.1 多目标优化框架**

**8.1.1研究背景与优化目标**

为验证问题二经验分段的合理性，本章引入CH指数对分段数k进行全局搜索，而非预设k=3。

研究需综合考量6项指标的关联与相互作用，实现三个核心目标：

1. 同段区间内数据差异最小
2. 不同段区间数据差异最大
3. Y与M、T的总拟合优度最大

本文将这三项要求转化为可计算的数学指标如下：段内差异最小与段间差异最大合并为Calinski-Harabasz指数（CH指数）的最大化问题，总拟合优度最大对应加权R²w的最大化问题。

两个指标经Min-Max归一化后以等权重合并为综合得分函数J，使不同目标在统一分析框架下具备可比性与可优化性。这样做有两个实际好处：CH指数本身就同时反映段内和段间两个方向，不需要分别优化再合并；归一化则解决了R²w和CH数值量级差异悬殊的问题，让两个目标在J函数里的贡献大致对等。

**8.1.2从问题二到问题三的内在逻辑**

问题二的最优分段结果（c₁=19、c₂=34），仅以加权总R²w为单一优化目标，未考虑各段在多指标空间中的内部一致性与相互区分度，因此无法保证分段在多指标空间中的结构合理性。事实上，高拟合优度并不等价于分段结构的合理性——一个分段方案可能在Y与M、T的拟合上表现良好，但段内个体在X1、X2、X3等辅助指标上仍存在高度异质性，说明该分段并未真正捕捉到生物生长的阶段性规律。

问题三在问题二基础上把全部6项指标都纳进来，三个目标一起优化。这一扩展也解释了为何问题三的最优分割点（c₁=21、c₂=34）与问题二存在细微差异：第一分割点从19小幅调整至21，反映了多指标结构信息对分段边界的细微修正，而第二分割点c₂=34在两个问题中保持一致，充分说明M≈34是一个在拟合优度和多指标区分度上均稳健的天然分界点。

**8.1.3目标实现**

目标一（拟合优度最大）：以各段加权总拟合优度R²w为指标，计算方式与问题二一致，即通过各段样本量占比加权求和得到总拟合优度：

其中，nₖ为第k段的样本量，n为总样本量。

目标二（段内差异最小、段间差异最大）：引入Calinski-Harabasz指数（CH指数）同时度量这两个目标[5]。CH指数定义为：

其中SS\_B为加权类间方差，SS\_W为类内总方差，k为分段数，n为总样本量。CH指数越大，说明段间差异越大、段内差异越小，与问题三的两个目标方向一致。计算时使用全部6项指标的标准化数据矩阵Z\_std，以消除不同指标间量纲差异的影响。

1. 数据标准化处理

CH指数基于欧氏距离，各维度需要可比。本文对6项指标进行Z-score标准化处理，将各指标统一变换至均值为0、标准差为1的标准化空间，消除了原始数据中量纲差异与数值尺度不一致的影响。在标准化空间中，各指标对欧氏距离的贡献权重趋于均等，不同生理指标之间的相对位置关系具有几何意义，因此以欧氏距离为基础的CH指数能够有效度量样本在多指标联合空间中的聚集程度与分离程度[4]。

结合5.3相关性分析结果可知，M与X3存在强相关（r=0.83），X2与X3存在中等相关（r=0.62），这意味着标准化空间中存在一定的信息冗余。但考虑到本文的核心目标是刻画不同M分段下样本的综合生理差异，保留全部6项指标能够更完整地反映个体的整体生长状态，故不做主成分降维处理，直接在原始标准化空间中计算CH指数。

1. CH指数在分段场景中的适用性

CH指数最初被提出用于评价自由聚类（如k-means）的质量，其核心逻辑是通过"类间离散度与类内紧密度之比"衡量聚类效果。本文的分段操作本质上是对M轴施加单调递增约束的一维受限聚类：每个分段区间对应一个连续的M值范围，段内样本在M轴上天然相邻，这与k-means中每个簇对应一个局部密集区域的几何含义高度一致。两者的核心区别仅在于本文的"簇边界"被约束为M轴上的切割点，而非自由的超平面——这一约束实际上使分段结果更具可解释性，也更符合题目对"M的合理区间划分"的要求。

为验证两者的一致性，可将k-means直接作用于标准化后的M单变量：在k=3时，k-means自动识别的三个簇中心分别为M=17.268、M=34.227、M=55.036，对应的自然分界区间与本文最优分割点（c₁=21、c₂=34）划定的三段高度吻合:

1. 簇中心17.3落在第一段(0,21]内部
2. 簇中心34.2落在第二段(21,34]内部
3. 簇中心55.0落在第三段(34,100)中部

两种方法对M轴结构的识别结论一致，这说明分段结果反映了数据本身的结构，而不是网格搜索搜出来的人为产物，CH指数用在这里是合理的。

**8.2 综合得分函数设计**

由于R²w与CH指数的数量级差异悬殊（R²w∈[0,1]，CH可达数百），直接加权会导致评分被CH主导。为此，对两个指标分别进行Min-Max归一化处理，再以等权重合并为综合得分函数：

其中R²w\_norm和CH\_norm分别为R²w和CH指数在所有候选方案中的归一化值。J越大，说明这个分段方案在拟合精度和多指标区分度上同时表现得更好。权重系数α=β=0.5的设定基于两个目标对分段合理性的评价具有同等重要性这一基本判断；问题四的灵敏度分析将进一步验证这一设定对结论的影响。

**8.3 最优分段数与分段区间**

对分段数k=2至6进行网格搜索，同时约束每段样本量不少于50个，归一化多目标评分结果如表9所示：

表 9不同分段数下的多目标评分结果

|  |  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- | --- |
| 分段数 k | R2w | CH 指数 | R2归一化 | CH归一化 | 综合得分J | 最优分割点 |
| 2 | 0.4064 | 254.43 | 0.2210 | 0.9750 | 0.5980 | [31] |
| 3 | 0.5709 | 159.32 | 0.7879 | 0.5528 | 0.6703 | [21,34] |
| 4 | 0.5880 | 113.90 | 0.8469 | 0.3511 | 0.5990 | [18,22,34] |
| 5 | 0.6324 | 96.19 | 1.0000 | 0.2725 | 0.6363 | [19,34,37,43] |
| 6 | 0.5819 | 88.14 | 0.8258 | 0.2368 | 0.5313 | [20,30,35,40,50] |

![综合评分对比图](data:image/png;base64...)

图12 综合评分对比图

由表9和图12可见，k=3时综合得分J最高（0.6703），为兼顾多目标的最优分段数。最优分段区间为：(0,21]、(21,34]、(34,100)，对应加权总R²w= 0.5709，CH指数=159.32。

表9中有一条清晰的规律：k越大，R²w越高，CH指数越低——分段越细，拟合越好，但段与段之间的区分度反而下降。具体来看，k=2时CH指数最高（254.43）但R²w最低（0.4064），说明两段划分虽然区分度好但拟合能力不足；k=3是两个目标之间的最佳折中。k=5时虽然R²w最高（0.6324），但CH指数已降至96.19，段间区分度大幅下降，导致综合得分反而低于k=3。

问题三的c₁=21与问题二的c₁=19差了2个单位，这个差异值得解释。这一差异并非算法误差，而是多目标框架引入额外信息后的合理调整：在M∈[19,21]的区间内共有约25个样本，这部分个体在纯拟合优度框架下被归入第一段，但在引入X1、X2、X3的多指标结构后，其综合生理特征与第二段（19,34]更为接近，因此在多目标优化下被重新划分至第二段，使第一段的段内一致性得到提升。c₂=34在两个问题中完全一致，说明M=34这个分界点不随优化目标的变化而移动，是数据本身固有的结构。

**8.4 鲁棒性分析**

表10和图13给出Bootstrap重采样（200次）的结果，用于检验k=3方案在不同子样本下是否稳定[6]。每次有放回地抽取1000个样本，在原最优分割点±5范围内重新搜索，记录最优c₁、c₂及对应R²w和CH指数。

表 10 Bootstrap鲁棒性分析结果

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| 指标 | 均值 | 标准差 | 2.5%分位数 | 97.5%分位数 |
| 分割点C1 | 20.220 | 1.122 | 18.000 | 21.000 |
| 分割点C2 | 35.935 | 2.421 | 34.000 | 39.000 |
| R2w | 0.646 | 0.083 | 0.522 | 0.829 |
| CH指数 | 164.204 | 18.730 | 130.231 | 201.042 |

![Bootstrap鲁棒性分析图](data:image/png;base64...)

图13 Bootstrap鲁棒性分析图

分割点c₁的200次Bootstrap结果高度集中于21附近（标准差仅1.122），95%置信区间为[18,21]，c₁几乎不动;c₂的标准差为2.421,95%置信区间[34,39]，c₂呈现双峰分布，主要集中于34和39两个位置，说明该分界点存在一定的结构性不确定性——M在34至39区间内样本分布较为稀疏，两个位置均可作为合理分界，不影响k=3方案的整体有效性。R²w的Bootstrap均值（0.646）略高于原始值（0.5709），95%置信区间为[0.522,0.829]，区间下限仍高于原始拟合优度，说明模型对子样本的变化不敏感。

200次重采样下，c₁几乎不动，c₂小幅波动但不出格，k=3的方案经得起检验。

1. **问题四：含成本约束的分段优化**

**9.1 检测成本-效率函数建模**

在实际检测中，分段区间的长度配置、分段数量的选择，会直接影响检测工作的经济成本与执行效率。本文在问题三的J函数基础上新增两项惩罚项来量化这一约束：

其中各项定义如下:

区间长度偏差惩罚P\_L：以等长分段的参考长度L₀=M值域/k为基准（其中k为当前分段数），通过计算各段实际长度与L₀的归一化均方偏差刻画长度不均匀性，公式为：

P\_L越大，各段长度越不均匀，检测成本越高。

分段数偏差惩罚P\_k：以问题三确定的最优分段数k₀=3为参考，惩罚偏离程度：

P\_k越大说明分段数偏离理想值越远，检测效率越低。

λ₁和λ₂分别控制长度惩罚和分段数惩罚的力度；α=β=0.5沿用问题三的等权设定。

λ₁=λ₂=0.3的选取依据灵敏度分析：扫描λ∈[0,0.8]发现最优k只在2和3之间切换，切换边界清晰，λ=0.3落在边界附近，使惩罚项贡献约占目标函数的20%~30%。两者取等值是因为没有先验理由偏向某一方。

**9.2 约束优化求解**

在λ₁=λ₂=0.3的基准设定下，各分段数对应的最优方案如下表：

表 11 含成本约束的最优分段结果

|  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| 分段数 k | R2w | CH | P\_L | 综合J\_cost | 最优分割点 |
| 2 | 0.4117 | 248.94 | 0.1461 | 0.5338 | [34] |
| 3 | 0.5709 | 159.32 | 0.6379 | 0.4189 | [21, 34] |
| 4 | 0.5445 | 135.43 | 0.4650 | 0.3210 | [22, 34, 50] |
| 5 | 0.5770 | 100.11 | 0.6724 | 0.1074 | [10, 19, 34, 52] |
| 6 | 0.5819 | 88.14 | 1.1459 | −0.2290 | [20, 30, 35, 40,50] |

与8.3节的k=3方案相比，引入成本约束后最优方案收缩为k=2（见表11），区间长度惩罚P\_L从0.6379降至0.1461，各段长度均匀性明显提高。

**9.3 灵敏度分析**

对λ₁和λ₂在[0,0.8]范围内以步长0.1进行全空间扫描，共计形成81组独立参数组合，分析惩罚系数变化对最优分段数和最优R²w的影响，得到灵敏度分析热力图，结果如图14所示。

![](data:image/png;base64...)

图14 灵敏度分析热力图

图14左侧显示：当λ₁=0（不施加长度惩罚）时，只要λ₂≥0.1，最优分段数即稳定为k=3，与问题三结果一致；当λ₂=0（不施加分段数惩罚）时，无论λ₁取[0,0.8]内任何值，最优分段数始终为k=2，说明长度惩罚对压低分段数的作用比分段数惩罚本身还强。整体来看，热力图呈现出明显的两区结构：当λ₁<0.2且λ₂≥0.1时，最优k=3；其余绝大多数参数组合下最优为k=2。这一边界清晰，说明结果对惩罚系数的变化具有较强的稳定性，不存在结果随参数小幅变化就剧烈跳跃的情况。

从右图最优R²w的灵敏度热力图可见，R²w同样呈现出与k对应的两区结构：k=3区域对应R²w≈0.57，k=2区域对应R²w≈0.41，两个区域内部数值高度均匀，边界过渡清晰。最优方案主要在k=2和k=3之间切换，不会在多个k值间频繁跳跃。

落到实际决策上：检测资源有限时取λ₁≥0.2或λ₂偏小，结果是k=2、c=34；资源充足、更看重阶段区分度时取λ₁<0.2且λ₂≥0.1，结果回到k=3、[21,34]。λ₁=λ₂=0.3落在两区边界附近，是一个不偏向任何一方的中性起点，使用者可以根据实际资源约束沿热力图边界调整。

简言之：检测成本是主要约束时选k=2、c=34；精度与阶段区分度优先时选k=3、[21,34]。

1. **总结**

![总流程图](data:image/png;base64...)

图15　生物生长规律研究建模总流程图

本文从数据探索出发，沿"整体建模→分段优化→多指标联合→成本约束"的递进脉络，对某生物1000个个体的6项生理指标做了逐步深入的定量分析。

在整体建模层面，Y对M与T的响应关系并非简单的线性叠加。为此，构建含M²T与MT²交叉项的三次扩展曲面模型，捕捉到了两者的二阶交互效应，训练集R²达到0.3290。但5折交叉验证揭示了均值R²仅为0.2933且折间标准差高达0.1600的不稳定性。究其根源，Y分布的极端尖峰性（峰度51.071）使少数高值离群个体对不同测试集的影响高度不对称。这不是模型失败，而是数据本身在告诉我们分段是必要的。

分段策略的引入显著改善了这一局面。固定分段将加权总R²从0.3290提升至0.4129；网格搜索进一步将最优分割点锁定在c₁=19、c₂=34，此时R²跃升至0.5740。中间段(19,34]的R²高达0.8065，Y与M、T的关系在这里最为规律；两端R²偏低，仅凭M与T两项指标已经到了上限。

问题三把这个判断落实为多目标优化。引入Calinski-Harabasz指数作为多指标空间中段间区分度与段内一致性的联合度量，经Min-Max归一化处理后与加权总R²w等权合并。对分段数k=2至6的系统搜索表明，k=3时综合得分J最高（0.6703），对应的最优分段区间为(0,21]、(21,34]、(34,100)。与问题二的微小差异（c₁从19调整至21）揭示了多指标结构信息对分段边界的细化作用，而c₂=34的跨问题稳定性则佐证了这一分界点的数据内生性。200次Bootstrap重采样显示c₁几乎不动，c₂小幅波动但不出格。

问题四把区间长度均匀性和分段数两个约束直接写进目标函数。在基准参数λ₁=λ₂=0.3的设定下，最优方案收缩为k=2，反映了成本约束对分段数的抑制效应。灵敏度分析显示结论在宽泛的参数范围内，最优分段数仅在k=2与k=3之间切换，且切换边界清晰，使用者可以直接对照热力图边界选方案。

回顾全文，有两点值得单独说明。第一，把CH指数引入分段优化，使分段方案同时考虑拟合精度和多指标区分度，比单纯看R²更贴近题目要求的"合理划分生长阶段"；第二，含成本惩罚的目标函数配合灵敏度分析，让不同资源约束下的分段决策有图可查、有据可依。

参考文献

[1] 茆诗松，程依明，濮晓龙．概率论与数理统计教程．北京：高等教育出版社，2011。

[2] 何晓群．多元统计分析．北京：中国人民大学出版社，2019。

[3] Akaike H. A new look at the statistical model identification. IEEE Transactions on Automatic Control, 20(6): 716-723, 1975.

[4] 薛毅，陈立萍．统计建模与R软件．北京：清华大学出版社，2007。

[5] Caliński T, Harabasz J. A dendrite method for cluster analysis. Communications in Statistics, 3(1): 1-27, 1974.

[6] Efron B, Tibshirani R J. An Introduction to the Bootstrap. New York: Chapman & Hall, 1993.

**附录 MATLAB代码**

%% 第五章 数据探索分析

%% Step 1: 导入数据

clear; clc; close all;

% 读取Excel数据

data = readtable('2026C附件数据.xlsx');

% 提取各变量

T = data.T;

M = data.M;

Y = data.Y;

X1 = data.X1;

X2 = data.X2;

X3 = data.X3;

% 确认导入成功

disp('数据导入成功，共读取行数：');

disp(height(data));

%% Step 2: 基本统计描述

vars = [T, M, Y, X1, X2, X3];

labels = {'T','M','Y','X1','X2','X3'};

% 计算统计量

n = size(vars, 1);

mn = min(vars);

mx = max(vars);

mu = mean(vars);

med = median(vars);

sd = std(vars);

sk = skewness(vars);

ku = kurtosis(vars);

% 在命令行打印统计表

fprintf('\n========== 基本统计描述 ==========\n');

fprintf('%-5s %8s %8s %8s %8s %8s %8s %8s\n', ...

'指标','最小值','最大值','均值','中位数','标准差','偏度','峰度');

fprintf('%s\n', repmat('-',1,65));

for i = 1:6

fprintf('%-5s %8.3f %8.3f %8.3f %8.3f %8.3f %8.3f %8.3f\n', ...

labels{i}, mn(i), mx(i), mu(i), med(i), sd(i), sk(i), ku(i));

end

fprintf('%s\n', repmat('-',1,65));

%% Step 3: 相关系数矩阵

R\_corr = corr(vars);

fprintf('\n========== Pearson相关系数矩阵 ==========\n');

fprintf('%-6s', ' ');

for i = 1:6; fprintf('%8s', labels{i}); end

fprintf('\n');

for i = 1:6

fprintf('%-6s', labels{i});

for j = 1:6; fprintf('%8.3f', R\_corr(i,j)); end

fprintf('\n');

end

%% 第六章 问题一：Y与M、T关系建模

%% Step 1: 候选模型比较（AIC/BIC选阶）

%% ===================================================

n = length(Y);

% 构建四个候选模型的特征矩阵

% 模型1：线性

X1\_m = [ones(n,1), M, T];

% 模型2：含交叉项

X2\_m = [ones(n,1), M, T, M.\*T];

% 模型3：完整二次曲面

X3\_m = [ones(n,1), M, T, M.^2, T.^2, M.\*T];

% 模型4：三次交叉扩展

X4\_m = [ones(n,1), M, T, M.^2, T.^2, M.\*T, M.^2.\*T, M.\*T.^2];

models = {X1\_m, X2\_m, X3\_m, X4\_m};

modnames = {'线性模型','含交叉项线性','完整二次曲面','三次交叉扩展'};

fprintf('\n========== 候选模型比较 ==========\n');

fprintf('%-16s %8s %10s %10s %6s\n','模型','R²','AIC','BIC','参数数');

fprintf('%s\n', repmat('-',1,58));

for i = 1:4

X = models{i};

p = size(X, 2);

b = X \ Y;

Yhat = X \* b;

res = Y - Yhat;

SS\_res = sum(res.^2);

SS\_tot = sum((Y - mean(Y)).^2);

R2 = 1 - SS\_res / SS\_tot;

sigma2 = SS\_res / n;

logL = -n/2 \* log(2\*pi\*sigma2) - SS\_res/(2\*sigma2);

AIC = -2\*logL + 2\*p;

BIC = -2\*logL + p\*log(n);

fprintf('%-16s %8.4f %10.2f %10.2f %6d\n', ...

modnames{i}, R2, AIC, BIC, p);

end

fprintf('%s\n', repmat('-',1,58));

%% Step 2: 用选定模型（三次交叉扩展）求解系数

X\_opt = [ones(n,1), M, T, M.^2, T.^2, M.\*T, M.^2.\*T, M.\*T.^2];

b\_opt = X\_opt \ Y;

fprintf('\n========== 模型参数估计 ==========\n');

fprintf('%-12s %12s\n','项','系数估计值');

fprintf('%s\n', repmat('-',1,28));

terms = {'常数项','M','T','M²','T²','M·T','M²·T','M·T²'};

for i = 1:8

fprintf('%-12s %12.6f\n', terms{i}, b\_opt(i));

end

fprintf('%s\n', repmat('-',1,28));

% 计算最终R²和调整R²

Yhat\_opt = X\_opt \* b\_opt;

res\_opt = Y - Yhat\_opt;

SS\_res = sum(res\_opt.^2);

SS\_tot = sum((Y - mean(Y)).^2);

R2\_opt = 1 - SS\_res / SS\_tot;

R2\_adj = 1 - (SS\_res/(n-8-1)) / (SS\_tot/(n-1));

fprintf('\nR² = %.4f\n', R2\_opt);

fprintf('调整R² = %.4f\n', R2\_adj);

fprintf('RMSE = %.4f\n', sqrt(SS\_res/n));

%% 验证：k-means与分段结果一致性

rng(42);

[idx\_km, C] = kmeans(M, 3, 'Replicates', 20);

centers = sort(C);

fprintf('\n========== k-means验证 ==========\n');

fprintf('k=3时三个簇中心（M单变量）：\n');

fprintf('簇中心1: %.3f\n', centers(1));

fprintf('簇中心2: %.3f\n', centers(2));

fprintf('簇中心3: %.3f\n', centers(3));

%% Step 2c: 5折交叉验证

fprintf('\n========== 5折交叉验证 ==========\n');

rng(42); % 固定随机种子，保证可重复性

K = 5;

n\_cv = length(Y);

idx\_rand = randperm(n\_cv);

fold\_size = floor(n\_cv / K);

R2\_cv = zeros(K, 1);

RMSE\_cv = zeros(K, 1);

for fold = 1:K

% 划分测试集和训练集

test\_start = (fold-1)\*fold\_size + 1;

test\_end = fold \* fold\_size;

test\_idx = idx\_rand(test\_start:test\_end);

train\_idx = setdiff(1:n\_cv, test\_idx);

% 训练集

M\_tr = M(train\_idx); T\_tr = T(train\_idx); Y\_tr = Y(train\_idx);

n\_tr = length(Y\_tr);

X\_tr = [ones(n\_tr,1), M\_tr, T\_tr, M\_tr.^2, T\_tr.^2, ...

M\_tr.\*T\_tr, M\_tr.^2.\*T\_tr, M\_tr.\*T\_tr.^2];

b\_cv = X\_tr \ Y\_tr;

% 测试集

M\_te = M(test\_idx); T\_te = T(test\_idx); Y\_te = Y(test\_idx);

n\_te = length(Y\_te);

X\_te = [ones(n\_te,1), M\_te, T\_te, M\_te.^2, T\_te.^2, ...

M\_te.\*T\_te, M\_te.^2.\*T\_te, M\_te.\*T\_te.^2];

Y\_pred = X\_te \* b\_cv;

% 计算测试集R²和RMSE

SS\_res = sum((Y\_te - Y\_pred).^2);

SS\_tot = sum((Y\_te - mean(Y\_te)).^2);

R2\_cv(fold) = 1 - SS\_res/SS\_tot;

RMSE\_cv(fold) = sqrt(SS\_res/n\_te);

fprintf('第%d折：R²=%.4f，RMSE=%.4f\n', fold, R2\_cv(fold), RMSE\_cv(fold));

end

fprintf('-----------------------------\n');

fprintf('CV均值：R²=%.4f，RMSE=%.4f\n', mean(R2\_cv), mean(RMSE\_cv));

fprintf('CV标准差：R²=%.4f，RMSE=%.4f\n', std(R2\_cv), std(RMSE\_cv));

R2\_train = 0.3290; % 直接写入训练集R²

fprintf('训练集R²=%.4f vs CV均值R²=%.4f，差值=%.4f\n',...

R2\_train, mean(R2\_cv), R2\_train-mean(R2\_cv));

%% 随机森林baseline对比

rng(42);

X\_all = [M, T];

n\_rf = length(Y);

% 5折交叉验证随机森林

fold\_size\_rf = floor(n\_rf/5);

idx\_rand\_rf = randperm(n\_rf);

R2\_rf = zeros(5,1);

for fold = 1:5

test\_start = (fold-1)\*fold\_size\_rf + 1;

test\_end = fold \* fold\_size\_rf;

test\_idx = idx\_rand\_rf(test\_start:test\_end);

train\_idx = setdiff(1:n\_rf, test\_idx);

X\_tr = X\_all(train\_idx,:);

Y\_tr = Y(train\_idx);

X\_te = X\_all(test\_idx,:);

Y\_te = Y(test\_idx);

% 训练随机森林

rf\_model = TreeBagger(100, X\_tr, Y\_tr, ...

'Method','regression',...

'MinLeafSize',5,...

'OOBPrediction','off');

Y\_pred\_rf = predict(rf\_model, X\_te);

SS\_res = sum((Y\_te - Y\_pred\_rf).^2);

SS\_tot = sum((Y\_te - mean(Y\_te)).^2);

R2\_rf(fold) = 1 - SS\_res/SS\_tot;

end

fprintf('\n========== 随机森林baseline ==========\n');

fprintf('各折R²：\n');

for i=1:5

fprintf('第%d折：R²=%.4f\n',i,R2\_rf(i));

end

fprintf('CV均值R²=%.4f，标准差=%.4f\n',mean(R2\_rf),std(R2\_rf));

fprintf('多项式模型CV均值R²=0.2933 vs 随机森林CV均值R²=%.4f\n',mean(R2\_rf));

%% 第七章 问题二：M的分段拟合分析

%% Step 1: 读取数据

clear; clc; close all;

data = readtable('2026C附件数据.xlsx');

T = data.T;

M = data.M;

Y = data.Y;

n = length(Y);

%% Step 2: 固定分段 (0,30] (30,50] (50,100)

cuts = [0, 30, 50, 100];

labels = {'(0,30]','(30,50]','(50,100)'};

fprintf('\n========== 固定分段拟合结果 ==========\n');

fprintf('%-10s %6s %10s %10s %10s %10s\n', ...

'分段','样本量','R²','调整R²','RMSE','权重');

fprintf('%s\n', repmat('-',1,62));

R2\_segs = zeros(1,3);

n\_segs = zeros(1,3);

res\_segs = cell(1,3);

Yhat\_segs = cell(1,3);

for k = 1:3

idx = M > cuts(k) & M <= cuts(k+1);

if k == 3

idx = M > cuts(k) & M < cuts(k+1);

end

Mk = M(idx); Tk = T(idx); Yk = Y(idx);

nk = sum(idx);

n\_segs(k) = nk;

% 每段用三次交叉扩展模型（与问题一保持一致）

Xk = [ones(nk,1), Mk, Tk, Mk.^2, Tk.^2, Mk.\*Tk, Mk.^2.\*Tk, Mk.\*Tk.^2];

bk = Xk \ Yk;

Yhat\_k = Xk \* bk;

res\_k = Yk - Yhat\_k;

SS\_res = sum(res\_k.^2);

SS\_tot = sum((Yk - mean(Yk)).^2);

R2k = 1 - SS\_res/SS\_tot;

R2adj = 1 - (SS\_res/(nk-8-1))/(SS\_tot/(nk-1));

RMSE = sqrt(SS\_res/nk);

R2\_segs(k) = R2k;

res\_segs{k} = res\_k;

Yhat\_segs{k} = Yhat\_k;

fprintf('%-10s %6d %10.4f %10.4f %10.4f %10.4f\n', ...

labels{k}, nk, R2k, R2adj, RMSE, nk/n);

end

% 计算加权总拟合优度

R2\_total = sum(R2\_segs .\* n\_segs) / n;

fprintf('%s\n', repmat('-',1,62));

fprintf('%-10s %6d %10.4f\n','加权总R²', n, R2\_total);

fprintf('%s\n', repmat('-',1,62));

%% Step 4: 最优分段搜索（网格搜索）

% 约束：每段样本量不少于50个

min\_n = 50;

% 搜索范围：分割点c1在(10,45)，c2在(c1+5, 80)，步长1

c1\_range = 10:1:45;

c2\_range = 15:1:80;

best\_R2 = -inf;

best\_c1 = 0;

best\_c2 = 0;

results = [];

fprintf('\n正在搜索最优分割点，请稍候...\n');

for c1 = c1\_range

for c2 = c2\_range

if c2 <= c1 + 5

continue;

end

idx1 = M > 0 & M <= c1;

idx2 = M > c1 & M <= c2;

idx3 = M > c2 & M < 100;

n1 = sum(idx1); n2 = sum(idx2); n3 = sum(idx3);

% 每段样本量约束

if n1 < min\_n || n2 < min\_n || n3 < min\_n

continue;

end

R2\_k = zeros(1,3);

segs = {idx1, idx2, idx3};

valid = true;

for k = 1:3

Mk = M(segs{k}); Tk = T(segs{k}); Yk = Y(segs{k});

nk = sum(segs{k});

Xk = [ones(nk,1), Mk, Tk, Mk.^2, Tk.^2, ...

Mk.\*Tk, Mk.^2.\*Tk, Mk.\*Tk.^2];

% 检查矩阵条件数，避免病态

if rank(Xk) < 8

valid = false; break;

end

bk = Xk \ Yk;

Yhat\_k = Xk \* bk;

SS\_res = sum((Yk - Yhat\_k).^2);

SS\_tot = sum((Yk - mean(Yk)).^2);

R2\_k(k) = 1 - SS\_res/SS\_tot;

end

if ~valid; continue; end

% 加权总R²

ns = [n1, n2, n3];

R2w = sum(R2\_k .\* ns) / n;

results = [results; c1, c2, n1, n2, n3, R2\_k, R2w];

if R2w > best\_R2

best\_R2 = R2w;

best\_c1 = c1;

best\_c2 = c2;

end

end

end

fprintf('搜索完成！\n');

fprintf('\n========== 最优分段结果 ==========\n');

fprintf('最优分割点：c1 = %d，c2 = %d\n', best\_c1, best\_c2);

fprintf('最优分段区间：(0,%d]、(%d,%d]、(%d,100)\n', ...

best\_c1, best\_c1, best\_c2, best\_c2);

fprintf('加权总R²：%.4f\n', best\_R2);

% 输出最优分段的详细结果

idx1 = M > 0 & M <= best\_c1;

idx2 = M > best\_c1 & M <= best\_c2;

idx3 = M > best\_c2 & M < 100;

ns = [sum(idx1), sum(idx2), sum(idx3)];

seg\_labels = {sprintf('(0,%d]',best\_c1), ...

sprintf('(%d,%d]',best\_c1,best\_c2), ...

sprintf('(%d,100)',best\_c2)};

fprintf('\n%-12s %6s %10s\n','分段','样本量','R²');

fprintf('%s\n',repmat('-',1,32));

segs = {idx1, idx2, idx3};

for k = 1:3

Mk = M(segs{k}); Tk = T(segs{k}); Yk = Y(segs{k});

nk = ns(k);

Xk = [ones(nk,1), Mk, Tk, Mk.^2, Tk.^2, ...

Mk.\*Tk, Mk.^2.\*Tk, Mk.\*Tk.^2];

bk = Xk \ Yk;

SS\_res = sum((Yk - Xk\*bk).^2);

SS\_tot = sum((Yk - mean(Yk)).^2);

R2k = 1 - SS\_res/SS\_tot;

fprintf('%-12s %6d %10.4f\n', seg\_labels{k}, nk, R2k);

end

fprintf('%s\n',repmat('-',1,32));

fprintf('%-12s %6d %10.4f\n','加权总R²', n, best\_R2);

%% Step 6: 最优分段各段残差统计

idx1 = M > 0 & M <= best\_c1;

idx2 = M > best\_c1 & M <= best\_c2;

idx3 = M > best\_c2 & M < 100;

segs\_best = {idx1, idx2, idx3};

fprintf('\n========== 最优分段各段残差标准差 ==========\n');

for k = 1:3

Mk = M(segs\_best{k}); Tk = T(segs\_best{k}); Yk = Y(segs\_best{k});

nk = sum(segs\_best{k});

Xk = [ones(nk,1), Mk, Tk, Mk.^2, Tk.^2, Mk.\*Tk, Mk.^2.\*Tk, Mk.\*Tk.^2];

bk = Xk \ Yk;

res\_k = Yk - Xk\*bk;

fprintf('第%d段残差标准差：%.4f\n', k, std(res\_k));

end

%% 第八章 问题三：多指标联合最优分段

clear; clc; close all;

data = readtable('2026C附件数据.xlsx');

T = data.T; M = data.M; Y = data.Y;

X1 = data.X1; X2 = data.X2; X3 = data.X3;

n = length(Y);

Z = [T, M, Y, X1, X2, X3];

Z\_std = (Z - mean(Z)) ./ std(Z);

alpha = 0.5; beta = 0.5;

min\_n = 50;

% k越大步长越粗，控制组合数

cands\_by\_k = {

10:1:80, ... % k=2，步长1

10:1:80, ... % k=3，步长1

10:2:80, ... % k=4，步长2

10:3:80, ... % k=5，步长3

10:5:80 ... % k=6，步长5

};

%% Step 1: 收集所有候选方案

all\_R2w = []; all\_CH = []; all\_k = []; all\_cuts = {};

fprintf('正在收集候选方案，请稍候...\n');

for k = 2:6

cands = cands\_by\_k{k-1};

combos = nchoosek(cands, k-1);

fprintf('k=%d，共 %d 个组合...\n', k, size(combos,1));

for ci = 1:size(combos,1)

cuts = [0, sort(combos(ci,:)), 100];

valid = true;

for seg = 1:k

idx = M > cuts(seg) & M <= cuts(seg+1);

if sum(idx) < min\_n; valid = false; break; end

end

if ~valid; continue; end

R2w = calc\_R2w(M, T, Y, cuts, n);

if R2w == -inf; continue; end

CH = calc\_CH(Z\_std, M, cuts, k, n);

all\_R2w(end+1) = R2w;

all\_CH(end+1) = CH;

all\_k(end+1) = k;

all\_cuts{end+1} = cuts;

end

fprintf('k=%d 完成\n', k);

end

fprintf('共收集 %d 个有效方案\n', length(all\_R2w));

%% Step 2: 归一化 + 综合得分

R2w\_norm = (all\_R2w - min(all\_R2w)) / (max(all\_R2w) - min(all\_R2w));

CH\_norm = (all\_CH - min(all\_CH)) / (max(all\_CH) - min(all\_CH));

J\_all = alpha \* R2w\_norm + beta \* CH\_norm;

%% Step 3: 每个k找最优

fprintf('\n====== 归一化多目标最优分段结果 ======\n');

fprintf('%-6s %8s %10s %8s %8s %10s %s\n', ...

'分段数','R²w','CH(原始)','R²归一','CH归一','综合J','最优分割点');

fprintf('%s\n', repmat('-',1,72));

best\_results = struct('k',{},'cuts',{},'R2w',{},'CH',{},'J',{});

for k = 2:6

kidx = find(all\_k == k);

if isempty(kidx); continue; end

[Jbest, loc] = max(J\_all(kidx));

gi = kidx(loc);

pts\_str = sprintf('%g ', all\_cuts{gi}(2:end-1));

fprintf('%-6d %8.4f %10.4f %8.4f %8.4f %10.4f [%s]\n', ...

k, all\_R2w(gi), all\_CH(gi), R2w\_norm(gi), CH\_norm(gi), ...

Jbest, strtrim(pts\_str));

best\_results(end+1) = struct('k',k,'cuts',all\_cuts{gi}, ...

'R2w',all\_R2w(gi),'CH',all\_CH(gi),'J',Jbest);

end

fprintf('%s\n', repmat('-',1,72));

J\_vals = [best\_results.J];

[~, bi] = max(J\_vals);

bk = best\_results(bi);

fprintf('\n最优分段数：k = %d\n', bk.k);

fprintf('最优分割点：[%s]\n', sprintf('%g ', bk.cuts(2:end-1)));

fprintf('R²w = %.4f，CH = %.4f，J = %.4f\n', bk.R2w, bk.CH, bk.J);

%% Step 5: Bootstrap鲁棒性分析

fprintf('\n正在进行Bootstrap鲁棒性分析（200次重采样）...\n');

B = 200;

boot\_cuts = zeros(B, 2);

boot\_R2w = zeros(B, 1);

boot\_CH = zeros(B, 1);

opt\_cuts = best\_results(bi).cuts; % 最优方案分割点

for b = 1:B

% 有放回抽样

boot\_idx = randsample(n, n, true);

Mb = M(boot\_idx); Tb = T(boot\_idx);

Yb = Y(boot\_idx);

Zb = Z\_std(boot\_idx,:);

nb = n;

best\_Jb = -inf;

best\_c1b = opt\_cuts(2);

best\_c2b = opt\_cuts(3);

% 在最优分割点附近±5范围内搜索

for c1 = max(10, opt\_cuts(2)-5) : min(40, opt\_cuts(2)+5)

for c2 = max(c1+5, opt\_cuts(3)-5) : min(75, opt\_cuts(3)+5)

cuts\_b = [0, c1, c2, 100];

valid = true;

for seg = 1:3

idx = Mb > cuts\_b(seg) & Mb <= cuts\_b(seg+1);

if sum(idx) < 30; valid = false; break; end

end

if ~valid; continue; end

R2w\_b = calc\_R2w(Mb, Tb, Yb, cuts\_b, nb);

if R2w\_b == -inf; continue; end

CH\_b = calc\_CH(Zb, Mb, cuts\_b, 3, nb);

% 用原始归一化参数近似评分

R2n = (R2w\_b - min(all\_R2w))/(max(all\_R2w)-min(all\_R2w));

CHn = (CH\_b - min(all\_CH)) /(max(all\_CH) -min(all\_CH));

Jb = 0.5\*R2n + 0.5\*CHn;

if Jb > best\_Jb

best\_Jb = Jb;

best\_c1b = c1;

best\_c2b = c2;

end

end

end

boot\_cuts(b,:) = [best\_c1b, best\_c2b];

boot\_R2w(b) = calc\_R2w(Mb, Tb, Yb, [0,best\_c1b,best\_c2b,100], nb);

boot\_CH(b) = calc\_CH(Zb, Mb, [0,best\_c1b,best\_c2b,100], 3, nb);

end

fprintf('Bootstrap完成！\n');

fprintf('\n========== 鲁棒性分析结果 ==========\n');

fprintf('%-12s %8s %8s %8s %8s\n','指标','均值','标准差','2.5%%','97.5%%');

fprintf('%s\n',repmat('-',1,48));

stats\_data = [boot\_cuts(:,1), boot\_cuts(:,2), boot\_R2w, boot\_CH];

stat\_names = {'分割点c1','分割点c2','R²w','CH指数'};

for i = 1:4

d = stats\_data(:,i);

fprintf('%-12s %8.3f %8.3f %8.3f %8.3f\n', ...

stat\_names{i}, mean(d), std(d), ...

quantile(d,0.025), quantile(d,0.975));

end

fprintf('%s\n',repmat('-',1,48));

%% ── 函数定义 ──

function R2w = calc\_R2w(M, T, Y, cuts, n)

k = length(cuts) - 1;

R2w = 0;

for i = 1:k

idx = M > cuts(i) & M <= cuts(i+1);

Mk = M(idx); Tk = T(idx); Yk = Y(idx);

nk = sum(idx);

if nk < 10; R2w = -inf; return; end

Xk = [ones(nk,1), Mk, Tk, Mk.^2, Tk.^2, ...

Mk.\*Tk, Mk.^2.\*Tk, Mk.\*Tk.^2];

if rank(Xk) < min(size(Xk)); R2w = -inf; return; end

bk = Xk \ Yk;

SS\_res = sum((Yk - Xk\*bk).^2);

SS\_tot = sum((Yk - mean(Yk)).^2);

if SS\_tot < 1e-10; R2w = -inf; return; end

R2w = R2w + (nk/n) \* (1 - SS\_res/SS\_tot);

end

end

function CH = calc\_CH(Z\_std, M, cuts, k, n)

grand\_mean = mean(Z\_std);

SS\_B = 0; SS\_W = 0;

for i = 1:k

idx = M > cuts(i) & M <= cuts(i+1);

nk = sum(idx);

if nk < 2; CH = 0; return; end

mu\_k = mean(Z\_std(idx,:));

SS\_B = SS\_B + nk \* sum((mu\_k - grand\_mean).^2);

SS\_W = SS\_W + sum(sum((Z\_std(idx,:) - mu\_k).^2));

end

if SS\_W < 1e-10; CH = 0; return; end

CH = (SS\_B/(k-1)) / (SS\_W/(n-k));

end

%% 第九章 问题四：含成本约束的分段优化

clear; clc; close all;

data = readtable('2026C附件数据.xlsx');

T = data.T; M = data.M; Y = data.Y;

X1 = data.X1; X2 = data.X2; X3 = data.X3;

n = length(Y);

Z = [T, M, Y, X1, X2, X3];

Z\_std = (Z - mean(Z)) ./ std(Z);

% ── 从问题三沿用的归一化基准值（直接写入，无需重新搜索）──

R2w\_min = 0.3361; R2w\_max = 0.6324;

CH\_min = 88.14; CH\_max = 254.43;

alpha = 0.5; beta = 0.5;

%% Step 1: 定义成本-效率惩罚函数

% 区间长度惩罚：每段长度偏离"理想长度L0"越远，成本越高

% 分段数惩罚：偏离"理想分段数k0"越远，效率越低

% L0 = M值域/k（等长分段的参考长度）

% k0 = 3（问题三最优结果）

M\_range = max(M) - min(M); % ≈ 88.75

k0 = 3; % 理想分段数

lambda1 = 0.3; % 区间长度惩罚系数

lambda2 = 0.3; % 分段数惩罚系数

% 综合目标函数（含惩罚）：

% J\_cost = alpha\*R2w\_norm + beta\*CH\_norm - lambda1\*P\_L - lambda2\*P\_k

% P\_L：区间长度偏差惩罚（各段长度与理想长度L0之差的平方和，归一化）

% P\_k：分段数偏差惩罚（(k-k0)²归一化）

fprintf('成本-效率惩罚函数定义完成\n');

fprintf('惩罚系数：lambda1(长度)=%.1f，lambda2(分段数)=%.1f\n',lambda1,lambda2);

%% Step 2: 带惩罚的多目标搜索（k=2~6）

min\_n = 50;

cands\_by\_k = {

10:1:80, ...

10:1:80, ...

10:2:80, ...

10:3:80, ...

10:5:80

};

% P\_k归一化基准（k=2~6对应(k-3)²）

Pk\_vals = (2:6 - k0).^2;

Pk\_max = max(Pk\_vals);

all\_R2w=[]; all\_CH=[]; all\_k=[]; all\_cuts={}; all\_Jcost=[];

fprintf('\n正在搜索（含成本约束），请稍候...\n');

for k = 2:6

cands = cands\_by\_k{k-1};

combos = nchoosek(cands, k-1);

L0 = M\_range / k; % 理想段长

P\_k = ((k - k0)^2) / max(Pk\_max, 1); % 分段数惩罚（归一化）

for ci = 1:size(combos,1)

cuts = [0, sort(combos(ci,:)), 100];

valid = true;

for seg = 1:k

idx = M > cuts(seg) & M <= cuts(seg+1);

if sum(idx) < min\_n; valid = false; break; end

end

if ~valid; continue; end

R2w = calc\_R2w(M, T, Y, cuts, n);

if R2w == -inf; continue; end

CH = calc\_CH(Z\_std, M, cuts, k, n);

% 归一化

R2w\_n = (R2w - R2w\_min)/(R2w\_max - R2w\_min);

R2w\_n = max(0, min(1, R2w\_n));

CH\_n = (CH - CH\_min) /(CH\_max - CH\_min);

CH\_n = max(0, min(1, CH\_n));

% 区间长度惩罚

seg\_lens = diff(cuts);

P\_L = sum((seg\_lens - L0).^2) / (k \* L0^2); % 归一化

J\_cost = alpha\*R2w\_n + beta\*CH\_n ...

- lambda1\*P\_L - lambda2\*P\_k;

all\_R2w(end+1) = R2w;

all\_CH(end+1) = CH;

all\_k(end+1) = k;

all\_cuts{end+1} = cuts;

all\_Jcost(end+1) = J\_cost;

end

fprintf('k=%d 完成\n',k);

end

fprintf('搜索完成，共 %d 个有效方案\n', length(all\_R2w));

%% Step 3: 每个k找最优，输出结果表

fprintf('\n====== 含成本约束的最优分段结果（lambda1=%.1f，lambda2=%.1f）======\n',...

lambda1, lambda2);

fprintf('%-6s %8s %8s %8s %12s %s\n',...

'分段数','R²w','CH','P\_L','综合J\_cost','最优分割点');

fprintf('%s\n',repmat('-',1,68));

best\_cost = struct('k',{},'cuts',{},'R2w',{},'CH',{},'J',{});

for k = 2:6

kidx = find(all\_k == k);

if isempty(kidx); continue; end

[Jbest, loc] = max(all\_Jcost(kidx));

gi = kidx(loc);

cuts\_g = all\_cuts{gi};

L0 = M\_range/k;

PL\_best = sum((diff(cuts\_g)-L0).^2)/(k\*L0^2);

pts\_str = sprintf('%g ', cuts\_g(2:end-1));

fprintf('%-6d %8.4f %8.2f %8.4f %12.4f [%s]\n',...

k, all\_R2w(gi), all\_CH(gi), PL\_best, Jbest, strtrim(pts\_str));

best\_cost(end+1) = struct('k',k,'cuts',cuts\_g,...

'R2w',all\_R2w(gi),'CH',all\_CH(gi),'J',Jbest);

end

fprintf('%s\n',repmat('-',1,68));

J\_c = [best\_cost.J];

[~,bi] = max(J\_c);

bc = best\_cost(bi);

fprintf('\n最优分段数：k = %d\n', bc.k);

fprintf('最优分割点：[%s]\n', sprintf('%g ', bc.cuts(2:end-1)));

fprintf('R²w=%.4f，CH=%.2f，J\_cost=%.4f\n', bc.R2w, bc.CH, bc.J);

%% Step 4: 灵敏度分析（扫描lambda1和lambda2）

fprintf('\n正在进行灵敏度分析...\n');

lambda\_vals = 0:0.1:0.8;

nl = length(lambda\_vals);

% 存储每组lambda下的最优k和J

sens\_k = zeros(nl, nl);

sens\_J = zeros(nl, nl);

sens\_R2 = zeros(nl, nl);

for li1 = 1:nl

for li2 = 1:nl

lam1 = lambda\_vals(li1);

lam2 = lambda\_vals(li2);

J\_temp = zeros(size(all\_R2w));

for i = 1:length(all\_R2w)

ki = all\_k(i);

cuts\_i= all\_cuts{i};

R2w\_n = max(0,min(1,(all\_R2w(i)-R2w\_min)/(R2w\_max-R2w\_min)));

CH\_n = max(0,min(1,(all\_CH(i) -CH\_min) /(CH\_max -CH\_min)));

L0 = M\_range/ki;

PL = sum((diff(cuts\_i)-L0).^2)/(ki\*L0^2);

Pk = ((ki-k0)^2)/max(Pk\_max,1);

J\_temp(i) = 0.5\*R2w\_n + 0.5\*CH\_n - lam1\*PL - lam2\*Pk;

end

% 找每个k下最优，再找全局最优k

best\_J\_per\_k = -inf(1,5);

best\_R2\_per\_k= zeros(1,5);

for k = 2:6

kidx = find(all\_k==k);

if isempty(kidx); continue; end

[bj,bl] = max(J\_temp(kidx));

best\_J\_per\_k(k-1) = bj;

best\_R2\_per\_k(k-1) = all\_R2w(kidx(bl));

end

[~,bki] = max(best\_J\_per\_k);

sens\_k(li1,li2) = bki + 1;

sens\_J(li1,li2) = best\_J\_per\_k(bki);

sens\_R2(li1,li2) = best\_R2\_per\_k(bki);

end

end

fprintf('灵敏度分析完成\n');

%% Step 6: 输出关键边界分析

fprintf('\n========== 灵敏度边界分析 ==========\n');

fprintf('lambda1=0（无长度惩罚）时，各lambda2对应最优k：\n');

for li2=1:nl

fprintf(' lambda2=%.1f -> k=%d\n',lambda\_vals(li2),sens\_k(1,li2));

end

fprintf('\nlambda2=0（无分段数惩罚）时，各lambda1对应最优k：\n');

for li1=1:nl

fprintf(' lambda1=%.1f -> k=%d\n',lambda\_vals(li1),sens\_k(li1,1));

end

%% ── 函数定义 ──

function R2w = calc\_R2w(M, T, Y, cuts, n)

k=length(cuts)-1; R2w=0;

for i=1:k

idx=M>cuts(i)&M<=cuts(i+1);

Mk=M(idx);Tk=T(idx);Yk=Y(idx);nk=sum(idx);

if nk<10;R2w=-inf;return;end

Xk=[ones(nk,1),Mk,Tk,Mk.^2,Tk.^2,Mk.\*Tk,Mk.^2.\*Tk,Mk.\*Tk.^2];

if rank(Xk)<min(size(Xk));R2w=-inf;return;end

bk=Xk\Yk;

SS\_res=sum((Yk-Xk\*bk).^2);SS\_tot=sum((Yk-mean(Yk)).^2);

if SS\_tot<1e-10;R2w=-inf;return;end

R2w=R2w+(nk/n)\*(1-SS\_res/SS\_tot);

end

end

function CH=calc\_CH(Z\_std,M,cuts,k,n)

grand\_mean=mean(Z\_std);SS\_B=0;SS\_W=0;

for i=1:k

idx=M>cuts(i)&M<=cuts(i+1);nk=sum(idx);

if nk<2;CH=0;return;end

mu\_k=mean(Z\_std(idx,:));

SS\_B=SS\_B+nk\*sum((mu\_k-grand\_mean).^2);

SS\_W=SS\_W+sum(sum((Z\_std(idx,:)-mu\_k).^2));

end

if SS\_W<1e-10;CH=0;return;end

CH=(SS\_B/(k-1))/(SS\_W/(n-k));

end